---
phase: 08-依赖升级
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - package.json
  - pnpm-lock.yaml
  - master 分支
autonomous: false
requirements:
  - DEPS-01
  - DEPS-02
  - DEPS-03
  - DEPS-04
  - DEPS-05
  - DEPS-06
  - DEPS-07

must_haves:
  truths:
    - "升级验证成功，依赖已合并到主分支 或 升级验证失败，分支已回退清理"
    - "master 分支保持稳定可工作状态"
    - "项目文档已更新（如合并成功）"
  artifacts:
    - path: ".git/refs/heads/master"
      provides: "主分支状态"
      contains: "merged commits"
    - path: ".planning/phases/08-依赖升级/08-03-SUMMARY.md"
      provides: "升级结果文档"
      min_lines: 50
    - path: "CHANGELOG.md"
      provides: "变更日志"
      contains: "依赖升级"
  key_links:
    - from: "deps/vite-8-upgrade"
      to: "master"
      via: "git merge 或 git branch -D"
      pattern: "分支合并或清理"

---

<objective>
根据验证结果决定合并升级分支到主分支，或回退并清理升级分支。

目的：确保主分支始终处于稳定可工作状态，成功则获得新版本依赖，失败则保持现状。
输出：合并后的 master 分支（成功）或清理后的环境（失败）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-依赖升级/08-CONTEXT.md
@.planning/phases/08-依赖升级/08-RESEARCH.md
@.planning/phases/08-依赖升级/08-02-SUMMARY.md
@.planning/STATE.md
@package.json
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>
    根据 08-02 的验证结果，决定是否合并升级分支。
  </decision>
  <context>
    需要评估以下验证结果：

    **成功标准（必须全部满足）：**
    1. TypeScript 类型检查通过（0 错误或 < 5 个可接受的非核心错误）
    2. 生产构建成功
    3. 所有 111 个单元测试通过
    4. E2E 测试通过（如果存在）
    5. 开发服务器能正常启动

    **失败标准（满足任一即回退）：**
    1. 类型检查失败（> 10 个错误，或核心逻辑类型错误）
    2. 生产构建失败
    3. 单元测试失败（> 5 个测试，或核心功能测试失败）
    4. E2E 测试失败（如果存在）
    5. 开发服务器无法启动
    6. 运行时错误或功能退化

    查看 08-02-SUMMARY.md 中的详细验证结果，做出决策。
  </context>
  <options>
    <option id="merge-success">
      <name>合并升级分支（验证成功）</name>
      <pros>
        - 获得 Vite 8.0.3 的性能提升（Rolldown，10-30x 构建速度）
        - 获得最新依赖的 Bug 修复和新特性
        - 为后续开发奠定更好的基础
      </pros>
      <cons>
        - 潜在的未知边缘问题（虽有测试保护）
        - 需要更新文档和 CHANGELOG
      </cons>
    </option>
    <option id="rollback-failure">
      <name>回退升级分支（验证失败）</name>
      <pros>
        - 保持主分支稳定性
        - 避免引入破坏性变更
        - 可以等待后续版本稳定再升级
      </pros>
      <cons>
        - 错过 Vite 8 的性能提升
        - 需要在未来重新尝试升级
      </cons>
    </option>
  </options>
  <resume-signal>
    输入 "merge-success" 或 "rollback-failure" 以继续。
  </resume-signal>
</task>

<task type="auto">
  <name>任务 1: 合并升级分支到 master（成功路径）</name>
  <files>
    .git/refs/heads/master
    package.json
    pnpm-lock.yaml
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/.planning/phases/08-依赖升级/08-02-SUMMARY.md（验证结果确认）
    - D:/Projects/demo/games/2048/package.json（当前分支的版本）
  </read_first>
  <action>
    **仅在选择 "merge-success" 后执行此任务。**

    将升级分支合并到 master 分支：

    1. 切换到 master 分支：
       ```bash
       git checkout master
       ```

    2. 确保 master 分支是最新的：
       ```bash
       git pull origin master
       ```

    3. 合并升级分支：
       ```bash
       git merge deps/vite-8-upgrade --no-ff -m "deps: 升级到 Vite 8.0.3 和相关依赖

       - 升级 Vite 7.3.1 → 8.0.3（Rolldown 集成）
       - 升级 Vue 3.5.29 → 3.5.31
       - 升级 TypeScript 5.9.3 → 6.0.2
       - 升级 Vitest 4.0.18 → 4.1.2
       - 升级 @vitejs/plugin-vue 6.0.4 → 6.0.5
       - 升级 vue-tsc 3.2.5 → 3.2.6
       - 升级 TailwindCSS 4.2.1 → 4.2.2
       - 升级 vite-plugin-vue-devtools 8.0.6 → 8.1.1
       - 升级 ESLint 10.0.2 → 10.1.0
       - 所有 111 个单元测试通过验证
       - 生产构建验证成功

       Co-Authored-By: Claude Code <noreply@anthropic.com>"
       ```

    4. 推送到远程仓库：
       ```bash
       git push origin master
       ```

    5. 删除已合并的升级分支：
       ```bash
       git branch -d deps/vite-8-upgrade
       ```

    6. 验证合并后的状态：
       ```bash
       git log -1 --oneline
       grep -E '"(vite|vue|typescript):' package.json
       ```
  </action>
  <verify>
    <automated>git branch --show-current | grep -q "master" && grep "vite.*8.0.3" D:/Projects/demo/games/2048/package.json && echo "合并成功" || echo "合并失败"</automated>
  </verify>
  <done>
    - 当前分支为 master
    - master 分支包含升级的依赖版本
    - 升级分支已删除
    - 合并提交已推送到远程仓库
  </done>
</task>

<task type="auto">
  <name>任务 2: 回退并清理升级分支（失败路径）</name>
  <files>
    .git/
    node_modules/
    pnpm-lock.yaml
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/.planning/phases/08-依赖升级/08-02-SUMMARY.md（失败原因）
    - D:/Projects/demo/games/2048/.planning/phases/08-依赖升级/08-RESEARCH.md（回退策略）
  </read_first>
  <action>
    **仅在选择 "rollback-failure" 后执行此任务。**

    清理升级分支并恢复到 master 分支的稳定状态：

    1. 切换到 master 分支（丢弃升级分支的更改）：
       ```bash
       git checkout master
       ```

    2. 确保使用原始版本的依赖：
       ```bash
       git pull origin master
       ```

    3. 删除升级分支（包括未提交的更改）：
       ```bash
       git branch -D deps/vite-8-upgrade
       ```

    4. 清理升级过程中安装的依赖：
       ```bash
       rm -rf node_modules pnpm-lock.yaml
       ```

    5. 重新安装原始版本的依赖：
       ```bash
       pnpm install
       ```

    6. 验证恢复到原始版本：
       ```bash
       grep -E '"(vite|vue|typescript):' package.json
       pnpm list --depth=0 | grep -E "vite|vue|typescript"
       ```

       应显示：
       - "vite": "^7.3.1"（原始版本）
       - "vue": "^3.5.29"（原始版本）
       - "typescript": "~5.9.3"（原始版本）

    7. 运行快速验证确保环境正常：
       ```bash
       pnpm type-check && pnpm test:unit src/core/__tests__/game.test.ts --run
       ```
  </action>
  <verify>
    <automated>git branch --show-current | grep -q "master" && grep "vite.*7.3.1" D:/Projects/demo/games/2048/package.json && pnpm type-check 2>&1 | grep -v "error TS" && echo "回退成功" || echo "回退失败"</automated>
  </verify>
  <done>
    - 当前分支为 master
    - package.json 恢复到原始版本
    - node_modules 重新安装原始依赖
    - pnpm type-check 和基础测试通过
    - 升级分支已完全删除
  </done>
</task>

<task type="auto">
  <name>任务 3: 更新项目文档（成功路径）</name>
  <files>
    CLAUDE.md
    CHANGELOG.md
    .planning/codebase/STACK.md
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/CLAUDE.md（当前技术栈描述）
    - D:/Projects/demo/games/2048/.planning/codebase/STACK.md（技术栈文档）
    - D:/Projects/demo/games/2048/.planning/phases/08-依赖升级/08-02-SUMMARY.md（升级详情）
  </read_first>
  <action>
    **仅在选择 "merge-success" 后执行此任务。**

    更新项目文档以反映新的依赖版本：

    1. 更新 CLAUDE.md 中的技术栈版本：
       - 将 "Vite 7.3.1" 改为 "Vite 8.0.3"
       - 将 "Vue 3.5.29" 改为 "Vue 3.5.31"
       - 将 "TypeScript 5.9.3" 改为 "TypeScript 6.0.2"
       - 将 "Vitest 4.0.18" 改为 "Vitest 4.1.2"
       - 将 "TailwindCSS 4.2.1" 改为 "TailwindCSS 4.2.2"

    2. 更新 STACK.md 中的依赖版本：
       - 同步更新所有相关版本号

    3. 创建或更新 CHANGELOG.md：
       ```markdown
       ## [1.2.0] - 2026-03-30

       ### 依赖升级
       - 升级 Vite 7.3.1 → 8.0.3（Rolldown 集成，构建速度提升 10-30x）
       - 升级 Vue 3.5.29 → 3.5.31
       - 升级 TypeScript 5.9.3 → 6.0.2
       - 升级 Vitest 4.0.18 → 4.1.2
       - 升级 @vitejs/plugin-vue 6.0.4 → 6.0.5
       - 升级 vue-tsc 3.2.5 → 3.2.6
       - 升级 TailwindCSS 4.2.1 → 4.2.2
       - 升级 vite-plugin-vue-devtools 8.0.6 → 8.1.1
       - 升级 ESLint 10.0.2 → 10.1.0

       ### 验证
       - 所有 111 个单元测试通过
       - 生产构建验证成功
       - TypeScript 类型检查通过
       ```
  </action>
  <verify>
    <automated>grep "Vite 8.0.3" D:/Projects/demo/games/2048/CLAUDE.md && grep "TypeScript 6.0.2" D:/Projects/demo/games/2048/.planning/codebase/STACK.md && test -f "D:/Projects/demo/games/2048/CHANGELOG.md" && echo "文档更新成功" || echo "文档更新失败"</automated>
  </verify>
  <done>
    - CLAUDE.md 中的技术栈版本已更新
    - STACK.md 中的依赖版本已更新
    - CHANGELOG.md 已创建或更新
    - 所有文档版本号与 package.json 一致
  </done>
</task>

<task type="auto">
  <name>任务 4: 最终验证和状态确认</name>
  <files>
    .planning/STATE.md
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/.planning/STATE.md（项目状态）
    - D:/Projects/demo/games/2048/package.json（当前版本）
  </read_first>
  <action>
    执行最终验证，确保项目处于预期状态：

    1. **如果是成功路径（merge-success）：**
       ```bash
       # 验证当前分支和版本
       git branch --show-current
       grep -E '"(vite|vue|typescript):' package.json

       # 快速验证构建和测试
       pnpm type-check
       pnpm build
       pnpm test:unit --run
       ```

    2. **如果是失败路径（rollback-failure）：**
       ```bash
       # 验证已回退到原始版本
       git branch --show-current
       grep -E '"(vite|vue|typescript):' package.json

       # 快速验证环境正常
       pnpm type-check
       pnpm test:unit src/core/__tests__/game.test.ts --run
       ```

    3. 更新 STATE.md，记录 Phase 8 完成状态：
       - 更新进度条（Phase 8: 1/1 计划完成）
       - 在技术栈中更新版本号（如成功）
       - 在会话连续性中记录升级结果
  </action>
  <verify>
    <automated>if grep -q "vite.*8.0.3" D:/Projects/demo/games/2048/package.json; then echo "升级成功"; else grep -q "vite.*7.3.1" D:/Projects/demo/games/2048/package.json && echo "回退成功"; fi</automated>
  </verify>
  <done>
    - 项目处于稳定可工作状态
    - 所有验证命令通过
    - STATE.md 已更新
    - 升级结果（成功或失败）已明确记录
  </done>
</task>

</tasks>

<verification>
完成本计划后，执行以下验证：

1. **分支状态验证**：
   ```bash
   git branch --show-current
   git branch -a
   ```
   应显示在 master 分支，升级分支已删除

2. **依赖版本验证**：
   ```bash
   grep -E '"(vite|vue|typescript):' package.json
   ```
   成功路径应显示新版本，失败路径应显示原始版本

3. **功能验证**：
   ```bash
   pnpm type-check && pnpm build && pnpm test:unit --run
   ```
   所有命令应成功执行

4. **文档验证**：
   ```bash
   grep "Vite 8.0.3" CLAUDE.md  # 成功路径
   test -f CHANGELOG.md         # 成功路径
   ```
</verification>

<success_criteria>
- [x] 根据验证结果做出合并或回退决策
- [x] 合并或回退操作完成
- [x] master 分支处于稳定可工作状态
- [x] 项目文档已更新（如成功）
- [x] STATE.md 已更新
- [x] 升级分支已清理
</success_criteria>

<output>
完成本计划后，创建 `.planning/phases/08-依赖升级/08-03-SUMMARY.md`，记录：
1. 最终决策（合并或回退）及依据
2. 执行的操作步骤
3. 最终状态验证结果
4. 遇到的问题（如有）和解决方案
5. 对后续阶段的影响和建议
6. Phase 8 完成总结
</output>
