---
phase: 03-用户界面
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - src/components/GameOverOverlay.vue
  - src/components/GameWonOverlay.vue
  - src/components/GameContainer.vue
autonomous: false
requirements:
  - UI-10
  - UI-11

must_haves:
  truths:
    - "游戏结束时显示游戏结束覆盖层"
    - "游戏胜利时显示胜利覆盖层"
    - "覆盖层可以关闭并继续游戏"
    - "覆盖层提供重新开始选项"
  artifacts:
    - path: "src/components/GameOverOverlay.vue"
      provides: "游戏结束覆盖层"
      min_lines: 80
      exports: ["GameOverOverlay"]
    - path: "src/components/GameWonOverlay.vue"
      provides: "游戏胜利覆盖层"
      min_lines: 80
      exports: ["GameWonOverlay"]
    - path: "src/components/GameContainer.vue"
      provides: "集成覆盖层组件"
      modifies: true
  key_links:
    - from: "src/components/GameOverOverlay.vue"
      to: "src/stores/game.ts"
      via: "useGameStore"
      pattern: "useGameStore"
    - from: "src/components/GameWonOverlay.vue"
      to: "src/stores/game.ts"
      via: "useGameStore"
      pattern: "useGameStore"
    - from: "src/components/GameContainer.vue"
      to: "src/components/GameOverOverlay.vue"
      via: "组件导入"
      pattern: "import GameOverOverlay from"
    - from: "src/components/GameContainer.vue"
      to: "src/components/GameWonOverlay.vue"
      via: "组件导入"
      pattern: "import GameWonOverlay from"
---

<objective>
创建游戏结束和胜利覆盖层组件，提供游戏状态反馈和重新开始选项

目的：在游戏结束或胜利时显示友好的覆盖层，提供继续游戏或重新开始的选项
输出：游戏结束和胜利覆盖层组件，集成到游戏容器中
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-用户界面/03-CONTEXT.md
@.planning/phases/03-用户界面/03-01-SUMMARY.md
@.planning/phases/03-用户界面/03-02-SUMMARY.md
@.planning/phases/03-用户界面/03-03-SUMMARY.md

@src/stores/game.ts
@src/core/types.ts
@src/components/GameContainer.vue

# 从用户决策中提取的关键要求
- 游戏结束/胜利覆盖层的设计和交互由 Claude 决定
- 暗色/霓虹风格
- 玻璃态效果
- 支持继续游戏（胜利后）

# Store 状态
- store.isGameOver: 游戏是否结束
- store.isGameWon: 游戏是否胜利
- store.score: 当前分数
- store.highScore: 最高分
- store.initialize(): 开始新游戏
- store.reset(): 重置游戏状态

# 游戏状态说明
- GameStatus.LOST: 游戏结束（无法移动）
- GameStatus.WON: 游戏胜利（达到 2048）
- 胜利后可以继续游戏（store.moveGrid 允许在 WON 状态下移动）
</context>

<tasks>

<task type="auto">
  <name>任务 1：创建游戏结束覆盖层组件（GameOverOverlay.vue）</name>
  <files>src/components/GameOverOverlay.vue</files>
  <action>
创建 `src/components/GameOverOverlay.vue` 组件，显示游戏结束界面：

**脚本部分：**
```vue
&lt;script setup lang="ts"&gt;
import { useGameStore } from '@/stores/game'

defineProps&lt;{
  visible: boolean
}&gt;()

const emit = defineEmits&lt;{
  close: []
  retry: []
}&gt;()

const store = useGameStore()

function handleClose() {
  emit('close')
}

function handleRetry() {
  store.initialize()
  emit('retry')
}
&lt;/script setup&gt;
```

**模板部分：**
```vue
&lt;template&gt;
  &lt;Transition name="overlay"&gt;
    &lt;div v-if="visible" class="overlay-mask"&gt;
      &lt;div class="overlay-content"&gt;
        &lt;div class="overlay-icon"&gt;
          &lt;svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"&gt;
            &lt;circle cx="12" cy="12" r="10"/&gt;
            &lt;line x1="15" y1="9" x2="9" y2="15"/&gt;
            &lt;line x1="9" y1="9" x2="15" y2="15"/&gt;
          &lt;/svg&gt;
        &lt;/div&gt;

        &lt;h2 class="overlay-title"&gt;游戏结束&lt;/h2&gt;

        &lt;p class="overlay-message"&gt;
          无法继续移动了
        &lt;/p&gt;

        &lt;div class="score-display"&gt;
          &lt;div class="score-item"&gt;
            &lt;span class="score-label"&gt;最终分数&lt;/span&gt;
            &lt;span class="score-value"&gt;{{ store.score }}&lt;/span&gt;
          &lt;/div&gt;

          &lt;div class="score-item"&gt;
            &lt;span class="score-label"&gt;最高分&lt;/span&gt;
            &lt;span class="score-value"&gt;{{ store.highScore }}&lt;/span&gt;
          &lt;/div&gt;
        &lt;/div&gt;

        &lt;div class="overlay-actions"&gt;
          &lt;button @click="handleClose" class="action-btn secondary-btn"&gt;
            关闭
          &lt;/button&gt;
          &lt;button @click="handleRetry" class="action-btn primary-btn"&gt;
            再试一次
          &lt;/button&gt;
        &lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/Transition&gt;
&lt;/template&gt;
```

**样式部分：**
```vue
&lt;style scoped&gt;
.overlay-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.8);
  backdrop-filter: blur(8px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  padding: 1rem;
}

.overlay-content {
  background: linear-gradient(135deg, rgba(15, 23, 42, 0.9), rgba(88, 28, 135, 0.9));
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 1rem;
  padding: 2rem;
  max-width: 400px;
  width: 100%;
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
  text-align: center;
}

.overlay-icon {
  display: flex;
  justify-content: center;
  margin-bottom: 1rem;
  color: #ef4444;
}

.overlay-title {
  font-size: 2rem;
  font-weight: 800;
  color: white;
  margin: 0 0 0.5rem 0;
}

.overlay-message {
  color: rgba(255, 255, 255, 0.7);
  font-size: 1rem;
  margin: 0 0 1.5rem 0;
}

.score-display {
  display: flex;
  gap: 1rem;
  justify-content: center;
  margin-bottom: 1.5rem;
}

.score-item {
  background-color: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border-radius: 0.5rem;
  padding: 1rem;
  min-width: 100px;
}

.score-label {
  display: block;
  color: rgba(255, 255, 255, 0.7);
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.score-value {
  display: block;
  color: white;
  font-size: 1.5rem;
  font-weight: 700;
  margin-top: 0.25rem;
}

.overlay-actions {
  display: flex;
  gap: 0.75rem;
  justify-content: center;
}

.action-btn {
  flex: 1;
  padding: 0.75rem 1.5rem;
  border-radius: 0.5rem;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s ease;
  border: none;
}

.primary-btn {
  background: linear-gradient(135deg, #22d3ee, #a855f7);
  color: white;
}

.primary-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 15px -3px rgba(34, 211, 238, 0.3);
}

.secondary-btn {
  background-color: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  color: white;
}

.secondary-btn:hover {
  background-color: rgba(255, 255, 255, 0.2);
}

/* Vue Transition 动画 */
.overlay-enter-active,
.overlay-leave-active {
  transition: opacity 0.3s ease;
}

.overlay-enter-from,
.overlay-leave-to {
  opacity: 0;
}

.overlay-enter-active .overlay-content,
.overlay-leave-active .overlay-content {
  transition: transform 0.3s ease;
}

.overlay-enter-from .overlay-content,
.overlay-leave-to .overlay-content {
  transform: scale(0.9);
}

/* 移动端适配 */
@media (max-width: 640px) {
  .overlay-content {
    padding: 1.5rem;
  }

  .overlay-title {
    font-size: 1.5rem;
  }

  .overlay-actions {
    flex-direction: column;
  }

  .action-btn {
    width: 100%;
  }
}
&lt;/style&gt;
```

**关键特性：**
- 全屏半透明遮罩（backdrop-blur）
- 游戏结束图标（红色 X）
- 显示最终分数和最高分
- 两个操作按钮：关闭、再试一次
- Vue Transition 进出场动画
- 暗色/霓虹风格配色
- 玻璃态效果
- 响应式设计（移动端）
  </action>
  <verify>
    <automated>
      # 组件文件存在检查
      test -f src/components/GameOverOverlay.vue &amp;&amp; echo "GameOverOverlay.vue exists"
    </automated>
  </verify>
  <done>GameOverOverlay.vue 组件已创建，显示游戏结束界面，提供关闭和重试选项</done>
</task>

<task type="auto">
  <name>任务 2：创建游戏胜利覆盖层组件（GameWonOverlay.vue）</name>
  <files>src/components/GameWonOverlay.vue</files>
  <action>
创建 `src/components/GameWonOverlay.vue` 组件，显示游戏胜利界面：

**脚本部分：**
```vue
&lt;script setup lang="ts"&gt;
import { useGameStore } from '@/stores/game'

defineProps&lt;{
  visible: boolean
}&gt;()

const emit = defineEmits&lt;{
  close: []
  retry: []
  continue: []
}&gt;()

const store = useGameStore()

function handleClose() {
  emit('close')
}

function handleRetry() {
  store.initialize()
  emit('retry')
}

function handleContinue() {
  emit('close')
}
&lt;/script setup&gt;
```

**模板部分：**
```vue
&lt;template&gt;
  &lt;Transition name="overlay"&gt;
    &lt;div v-if="visible" class="overlay-mask"&gt;
      &lt;div class="overlay-content"&gt;
        &lt;div class="overlay-icon"&gt;
          &lt;svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"&gt;
            &lt;path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/&gt;
            &lt;polyline points="22 4 12 14.01 9 11.01"/&gt;
          &lt;/svg&gt;
        &lt;/div&gt;

        &lt;h2 class="overlay-title"&gt;恭喜胜利！&lt;/h2&gt;

        &lt;p class="overlay-message"&gt;
          你达到了 2048！太棒了！
        &lt;/p&gt;

        &lt;div class="score-display"&gt;
          &lt;div class="score-item"&gt;
            &lt;span class="score-label"&gt;最终分数&lt;/span&gt;
            &lt;span class="score-value"&gt;{{ store.score }}&lt;/span&gt;
          &lt;/div&gt;

          &lt;div class="score-item"&gt;
            &lt;span class="score-label"&gt;最高分&lt;/span&gt;
            &lt;span class="score-value"&gt;{{ store.highScore }}&lt;/span&gt;
          &lt;/div&gt;
        &lt;/div&gt;

        &lt;div class="overlay-actions"&gt;
          &lt;button @click="handleClose" class="action-btn secondary-btn"&gt;
            继续游戏
          &lt;/button&gt;
          &lt;button @click="handleRetry" class="action-btn primary-btn"&gt;
            新游戏
          &lt;/button&gt;
        &lt;/div&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/Transition&gt;
&lt;/template&gt;
```

**样式部分：**
```vue
&lt;style scoped&gt;
.overlay-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.8);
  backdrop-filter: blur(8px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  padding: 1rem;
}

.overlay-content {
  background: linear-gradient(135deg, rgba(15, 23, 42, 0.9), rgba(88, 28, 135, 0.9));
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 1rem;
  padding: 2rem;
  max-width: 400px;
  width: 100%;
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
  text-align: center;
}

.overlay-icon {
  display: flex;
  justify-content: center;
  margin-bottom: 1rem;
  color: #22c55e;
}

.overlay-title {
  font-size: 2rem;
  font-weight: 800;
  background: linear-gradient(135deg, #22d3ee, #a855f7);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin: 0 0 0.5rem 0;
}

.overlay-message {
  color: rgba(255, 255, 255, 0.7);
  font-size: 1rem;
  margin: 0 0 1.5rem 0;
}

.score-display {
  display: flex;
  gap: 1rem;
  justify-content: center;
  margin-bottom: 1.5rem;
}

.score-item {
  background-color: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border-radius: 0.5rem;
  padding: 1rem;
  min-width: 100px;
}

.score-label {
  display: block;
  color: rgba(255, 255, 255, 0.7);
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.score-value {
  display: block;
  color: white;
  font-size: 1.5rem;
  font-weight: 700;
  margin-top: 0.25rem;
}

.overlay-actions {
  display: flex;
  gap: 0.75rem;
  justify-content: center;
}

.action-btn {
  flex: 1;
  padding: 0.75rem 1.5rem;
  border-radius: 0.5rem;
  font-size: 1rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s ease;
  border: none;
}

.primary-btn {
  background: linear-gradient(135deg, #22d3ee, #a855f7);
  color: white;
}

.primary-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 15px -3px rgba(34, 211, 238, 0.3);
}

.secondary-btn {
  background-color: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  color: white;
}

.secondary-btn:hover {
  background-color: rgba(255, 255, 255, 0.2);
}

/* Vue Transition 动画 */
.overlay-enter-active,
.overlay-leave-active {
  transition: opacity 0.3s ease;
}

.overlay-enter-from,
.overlay-leave-to {
  opacity: 0;
}

.overlay-enter-active .overlay-content,
.overlay-leave-active .overlay-content {
  transition: transform 0.3s ease;
}

.overlay-enter-from .overlay-content,
.overlay-leave-to .overlay-content {
  transform: scale(0.9);
}

/* 移动端适配 */
@media (max-width: 640px) {
  .overlay-content {
    padding: 1.5rem;
  }

  .overlay-title {
    font-size: 1.5rem;
  }

  .overlay-actions {
    flex-direction: column;
  }

  .action-btn {
    width: 100%;
  }
}
&lt;/style&gt;
```

**关键特性：**
- 全屏半透明遮罩（backdrop-blur）
- 胜利图标（绿色对钩）
- 显示最终分数和最高分
- 两个操作按钮：继续游戏、新游戏
- Vue Transition 进出场动画
- 暗色/霓虹风格配色
- 玻璃态效果
- 响应式设计（移动端）
- 支持继续游戏（关闭覆盖层后可以继续玩）

**与 GameOverOverlay 的区别：**
- 图标颜色：绿色 vs 红色
- 标题：恭喜胜利 vs 游戏结束
- 消息：鼓励语 vs 无法继续
- 按钮：继续游戏 + 新游戏 vs 关闭 + 再试一次
  </action>
  <verify>
    <automated>
      # 组件文件存在检查
      test -f src/components/GameWonOverlay.vue &amp;&amp; echo "GameWonOverlay.vue exists"
    </automated>
  </verify>
  <done>GameWonOverlay.vue 组件已创建，显示游戏胜利界面，提供继续游戏和新游戏选项</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>游戏结束和胜利覆盖层组件（GameOverOverlay.vue 和 GameWonOverlay.vue）已创建，包含完整的样式和交互逻辑</what-built>
  <how-to-verify>
验证步骤：

1. **触发游戏结束**：
   - 运行开发服务器：`npm run dev`
   - 玩游戏直到无法移动（网格填满且无法合并）
   - 验证游戏结束覆盖层显示
   - 验证显示最终分数和最高分
   - 验证"关闭"和"再试一次"按钮可用
   - 点击"再试一次"，验证游戏重新开始
   - 点击"关闭"，验证覆盖层关闭

2. **触发游戏胜利**：
   - 刷新页面，开始新游戏
   - 玩游戏直到达到 2048（可能需要使用控制台快速设置）
   - 验证胜利覆盖层显示
   - 验证显示最终分数和最高分
   - 验证"继续游戏"和"新游戏"按钮可用
   - 点击"继续游戏"，验证覆盖层关闭且可以继续玩
   - 点击"新游戏"，验证游戏重新开始

3. **样式验证**：
   - 验证覆盖层样式符合暗色/霓虹风格
   - 验证玻璃态效果显示正常
   - 验证动画流畅（进出场动画）
   - 验证响应式布局（桌面和移动端）

4. **交互验证**：
   - 验证按钮悬停效果
   - 验证按钮点击响应
   - 验证覆盖层可以通过点击"关闭"关闭
  </how-to-verify>
  <resume-signal>输入 "approved" 表示验证通过，或描述发现的问题</resume-signal>
</task>

<task type="auto">
  <name>任务 4：集成覆盖层到 GameContainer</name>
  <files>src/components/GameContainer.vue</files>
  <action>
更新 `src/components/GameContainer.vue`，集成游戏结束和胜利覆盖层：

**脚本部分：**
```vue
&lt;script setup lang="ts"&gt;
import { ref, computed, onMounted } from 'vue'
import { useGameStore } from '@/stores/game'
import GameHeader from './GameHeader.vue'
import GameBoard from './GameBoard.vue'
import GameOverOverlay from './GameOverOverlay.vue'
import GameWonOverlay from './GameWonOverlay.vue'

const store = useGameStore()

// 覆盖层显示状态
const showGameOver = ref(false)
const showGameWon = ref(false)

// 监听游戏状态变化
const isGameOver = computed(() =&gt; store.isGameOver)
const isGameWon = computed(() =&gt; store.isGameWon)

// 监听游戏结束状态
function checkGameOver() {
  if (isGameOver.value && store.status === 'lost') {
    showGameOver.value = true
  }
}

// 监听游戏胜利状态
function checkGameWon() {
  if (isGameWon.value && store.status === 'won' && !showGameWon.value) {
    // 只在第一次胜利时显示，避免重复显示
    showGameWon.value = true
  }
}

// 使用 watch 监听状态变化
import { watch } from 'vue'

watch(isGameOver, () =&gt; {
  checkGameOver()
})

watch(isGameWon, () =&gt; {
  checkGameWon()
})

// 覆盖层事件处理
function handleOverlayClose() {
  showGameOver.value = false
  showGameWon.value = false
}

function handleOverlayRetry() {
  showGameOver.value = false
  showGameWon.value = false
  // initialize() 已在覆盖层组件中调用
}

// 初始化游戏
onMounted(() =&gt; {
  if (store.status === 'idle') {
    store.initialize()
  }
})
&lt;/script setup&gt;
```

**模板部分：**
```vue
&lt;template&gt;
  &lt;div class="game-container"&gt;
    &lt;!-- 头部区域：分数和控制按钮 --&gt;
    &lt;header class="game-header"&gt;
      &lt;GameHeader /&gt;
    &lt;/header&gt;

    &lt;!-- 游戏网格区域 --&gt;
    &lt;main class="game-board-wrapper"&gt;
      &lt;GameBoard /&gt;
    &lt;/main&gt;

    &lt;!-- 控制按钮区域（保留用于其他功能） --&gt;
    &lt;footer class="game-controls"&gt;
      &lt;!-- 未来可能添加：排行榜按钮、设置按钮等 --&gt;
    &lt;/footer&gt;

    &lt;!-- 游戏结束覆盖层 --&gt;
    &lt;GameOverOverlay
      :visible="showGameOver"
      @close="handleOverlayClose"
      @retry="handleOverlayRetry"
    /&gt;

    &lt;!-- 游戏胜利覆盖层 --&gt;
    &lt;GameWonOverlay
      :visible="showGameWon"
      @close="handleOverlayClose"
      @retry="handleOverlayRetry"
      @continue="handleOverlayClose"
    /&gt;
  &lt;/div&gt;
&lt;/template&gt;
```

**样式部分（保持不变）：**
```vue
&lt;style scoped&gt;
/* 现有样式保持不变 */
&lt;/style scoped&gt;
```

**关键变更：**
- 导入 GameOverOverlay 和 GameWonOverlay 组件
- 添加 showGameOver 和 showGameWon 响应式状态
- 使用 watch 监听 isGameOver 和 isGameWon 变化
- 实现覆盖层事件处理函数
- 在模板中渲染覆盖层组件

**覆盖层显示逻辑：**
- 游戏结束时自动显示 GameOverOverlay
- 游戏胜利时自动显示 GameWonOverlay（只显示一次）
- 关闭覆盖层后可以继续游戏（胜利后）
- 点击"再试一次"/"新游戏"后重置覆盖层状态

**胜利后继续游戏：**
- 覆盖层关闭后，store.status 保持为 'won'
- store.moveGrid 允许在 'won' 状态下继续移动
- 覆盖层不会再次显示（showGameWon 只在第一次胜利时为 true）
  </action>
  <verify>
    <automated>
      # 检查覆盖层集成
      grep -q "GameOverOverlay\|GameWonOverlay" src/components/GameContainer.vue &amp;&amp;
      grep -q "showGameOver\|showGameWon" src/components/GameContainer.vue &amp;&amp;
      echo "Overlays integrated into GameContainer"
    </automated>
  </verify>
  <done>覆盖层已集成到 GameContainer，游戏结束和胜利时自动显示，支持关闭和重新开始</done>
</task>

</tasks>

<verification>
## 整体验证标准

1. **组件验证**
   - [ ] GameOverOverlay.vue 组件存在且格式正确
   - [ ] GameWonOverlay.vue 组件存在且格式正确
   - [ ] GameContainer.vue 正确导入和使用覆盖层组件
   - [ ] TypeScript 类型检查通过

2. **功能验证**
   - [ ] 游戏结束时显示游戏结束覆盖层
   - [ ] 游戏胜利时显示胜利覆盖层
   - [ ] 覆盖层显示正确的分数和最高分
   - [ ] "再试一次"/"新游戏"按钮可以重新开始游戏
   - [ ] "关闭"/"继续游戏"按钮可以关闭覆盖层
   - [ ] 胜利后可以继续游戏

3. **样式验证**
   - [ ] 覆盖层样式符合暗色/霓虹风格
   - [ ] 玻璃态效果显示正常
   - [ ] 图标颜色正确（胜利=绿色，结束=红色）
   - [ ] 响应式布局（桌面和移动端）
   - [ ] 按钮有悬停效果

4. **动画验证**
   - [ ] 覆盖层进出场动画流畅
   - [ ] 动画时长合理（300ms）
   - [ ] 动画无卡顿

5. **交互验证**
   - [ ] 覆盖层可以通过按钮关闭
   - [ ] 覆盖层可以通过事件重新开始游戏
   - [ ] 覆盖层显示时阻止背景交互（遮罩层）
</verification>

<success_criteria>
1. GameOverOverlay.vue 和 GameWonOverlay.vue 组件创建完成
2. GameContainer.vue 已更新，集成覆盖层组件
3. 游戏结束和胜利时覆盖层自动显示
4. 覆盖层提供关闭和重新开始选项
5. 胜利后可以继续游戏
6. 所有 12 个 UI 需求已完成（UI-01 到 UI-12）
7. Phase 3 所有计划已完成
</success_criteria>

<output>
完成此计划后，创建 `.planning/phases/03-用户界面/03-05-SUMMARY.md`

**Phase 3 完成后，更新 STATE.md 和 ROADMAP.md，标记 Phase 3 为完成状态。**
</output>
