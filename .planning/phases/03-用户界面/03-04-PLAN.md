---
phase: 03-用户界面
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/components/GameBoard.vue
  - src/composables/useGameControls.ts
autonomous: true
requirements:
  - UI-09

must_haves:
  truths:
    - "键盘方向键可以控制方块移动"
    - "移动端触摸滑动可以控制方块移动"
    - "触摸滑动阈值合理（不会误触）"
    - "斜向滑动自动判断主体方向"
  artifacts:
    - path: "src/composables/useGameControls.ts"
      provides: "游戏控制 composable（键盘 + 触摸）"
      min_lines: 80
      exports: ["useGameControls"]
    - path: "src/components/GameBoard.vue"
      provides: "集成游戏控制"
      modifies: true
  key_links:
    - from: "src/composables/useGameControls.ts"
      to: "@vueuse/core"
      via: "useSwipe"
      pattern: "useSwipe"
    - from: "src/composables/useGameControls.ts"
      to: "src/stores/game.ts"
      via: "useGameStore"
      pattern: "useGameStore"
    - from: "src/components/GameBoard.vue"
      to: "src/composables/useGameControls.ts"
      via: "composable 导入"
      pattern: "useGameControls"
---

<objective>
实现键盘和触摸控制，支持桌面端键盘操作和移动端触摸滑动

目的：让玩家可以通过键盘方向键或触摸滑动控制游戏
输出：完整的游戏控制系统，支持键盘和触摸输入
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-用户界面/03-CONTEXT.md
@.planning/phases/03-用户界面/03-01-SUMMARY.md
@.planning/phases/03-用户界面/03-02-SUMMARY.md

@src/stores/game.ts
@src/core/types.ts
@src/components/GameBoard.vue

# 从用户决策中提取的关键要求
- 触摸检测：VueUse useSwipe - 使用 VueUse 的 useSwipe composable
- 滑动阈值：由 Claude 决定（根据用户体验和误触风险选择阈值）
- 输入优先级：同时生效 - 触摸和键盘同时可用，无冲突
- 斜向滑动：只允许正交方向 - 计算滑动的主体方向，选择水平或垂直

# 技术栈
- VueUse 的 useSwipe composable
- 键盘事件监听（keydown）
</context>

<tasks>

<task type="auto">
  <name>任务 1：创建游戏控制 composable（useGameControls.ts）</name>
  <files>src/composables/useGameControls.ts</files>
  <action>
创建 `src/composables/useGameControls.ts`，封装键盘和触摸控制逻辑：

**脚本部分：**
```typescript
import { onMounted, onUnmounted } from 'vue'
import { useSwipe } from '@vueuse/core'
import { useGameStore } from '@/stores/game'
import type { Direction } from '@/core/types'

/**
 * 游戏控制 composable
 * 提供键盘方向键和触摸滑动控制
 */
export function useGameControls(targetRef: Ref&lt;HTMLElement | undefined&gt;) {
  const store = useGameStore()

  // ===== 键盘控制 =====
  function handleKeydown(event: KeyboardEvent) {
    // 如果游戏已结束，不响应键盘输入
    if (store.status === 'lost' || store.status === 'won') {
      return
    }

    const keyMap: Record&lt;string, Direction&gt; = {
      'ArrowUp': 'UP',
      'ArrowDown': 'DOWN',
      'ArrowLeft': 'LEFT',
      'ArrowRight': 'RIGHT',
      'w': 'UP',
      's': 'DOWN',
      'a': 'LEFT',
      'd': 'RIGHT',
      'W': 'UP',
      'S': 'DOWN',
      'A': 'LEFT',
      'D': 'RIGHT',
    }

    const direction = keyMap[event.key]
    if (direction) {
      event.preventDefault() // 防止页面滚动
      store.moveGrid(direction)
    }
  }

  // ===== 触摸控制 =====
  // 滑动阈值（Claude's Discretion）
  // 50px 是一个合理的阈值：
  // - 太小（<30px）：容易误触，普通点击也会触发
  // - 太大（>80px）：需要滑动很长距离，体验差
  // 50px 在两者之间平衡，既避免误触又保持灵敏度
  const SWIPE_THRESHOLD = 50

  const { lengthX, lengthY } = useSwipe(targetRef, {
    threshold: SWIPE_THRESHOLD,
    passive: false,
    onSwipe() {
      // 计算滑动方向
      const absX = Math.abs(lengthX.value)
      const absY = Math.abs(lengthY.value)

      let direction: Direction | null = null

      // 判断主体方向（水平或垂直）
      if (absX > absY) {
        // 水平方向占主导
        if (lengthX.value > 0) {
          direction = 'RIGHT'
        } else {
          direction = 'LEFT'
        }
      } else {
        // 垂直方向占主导
        if (lengthY.value > 0) {
          direction = 'DOWN'
        } else {
          direction = 'UP'
        }
      }

      // 执行移动
      if (direction && store.status === 'playing') {
        store.moveGrid(direction)
      }
    },
  })

  // ===== 生命周期管理 =====
  onMounted(() => {
    // 添加键盘事件监听
    window.addEventListener('keydown', handleKeydown)
  })

  onUnmounted(() => {
    // 移除键盘事件监听
    window.removeEventListener('keydown', handleKeydown)
  })

  return {
    // 导出滑动距离（用于调试或可视化）
    lengthX,
    lengthY,
  }
}
```

**关键特性：**
- **键盘控制**：
  - 支持方向键（ArrowUp, ArrowDown, ArrowLeft, ArrowRight）
  - 支持 WASD 键（游戏玩家习惯）
  - 阻止默认行为（防止页面滚动）
  - 游戏结束时不响应输入

- **触摸控制**：
  - 使用 VueUse 的 useSwipe composable
  - 滑动阈值 50px（平衡灵敏度和误触风险）
  - 自动判断主体方向（比较水平/垂直距离）
  - 只允许正交方向（4个方向）
  - 游戏结束时不响应输入

- **输入优先级**：
  - 键盘和触摸同时可用
  - 无冲突，独立处理
  - 在组件挂载/卸载时管理事件监听器

**技术决策（Claude's Discretion）：**
- 滑动阈值：50px
  - 太小（<30px）：误触风险高
  - 太大（>80px）：用户体验差
  - 50px 是最佳平衡点

- 主体方向判断：比较 abs(lengthX) 和 abs(lengthY)
  - 水平距离 > 垂直距离 → 水平方向
  - 否则 → 垂直方向
  - 确保斜向滑动也能正确识别

**类型定义：**
需要确保从 Vue 导入 Ref 类型
  </action>
  <verify>
    <automated>
      # composable 文件存在检查
      test -f src/composables/useGameControls.ts &amp;&amp; echo "useGameControls.ts exists"
    </automated>
  </verify>
  <done>useGameControls composable 已创建，支持键盘方向键、WASD 和触摸滑动控制，阈值和方向判断合理</done>
</task>

<task type="auto">
  <name>任务 2：集成游戏控制到 GameBoard</name>
  <files>src/components/GameBoard.vue</files>
  <action>
更新 `src/components/GameBoard.vue`，集成游戏控制：

**脚本部分：**
```vue
&lt;script setup lang="ts"&gt;
import { computed, ref } from 'vue'
import { useGameStore } from '@/stores/game'
import Tile from './Tile.vue'
import { useGameControls } from '@/composables/useGameControls'

const store = useGameStore()

// 游戏板容器的 ref（用于触摸滑动检测）
const boardRef = ref&lt;HTMLElement&gt;()

// 启用游戏控制（键盘 + 触摸）
useGameControls(boardRef)

// 将 4x4 网格展平为一维数组，便于 v-for 渲染
const tiles = computed(() =&gt; {
  const result: Array&lt;{ value: number; row: number; col: number; id: string }&gt; = []
  for (let row = 0; row &lt; 4; row++) {
    for (let col = 0; col &lt; 4; col++) {
      result.push({
        value: store.grid[row][col],
        row,
        col,
        id: `tile-${row}-${col}`
      })
    }
  }
  return result
})
&lt;/script setup&gt;
```

**模板部分（添加 ref）：**
```vue
&lt;template&gt;
  &lt;div class="game-board" ref="boardRef"&gt;
    &lt;div class="grid-container"&gt;
      &lt;Tile
        v-for="tile in tiles"
        :key="tile.id"
        :value="tile.value"
        :row="tile.row"
        :col="tile.col"
        class="grid-item"
      /&gt;
    &lt;/div&gt;
  &lt;/div&gt;
&lt;/template&gt;
```

**样式部分（保持不变）：**
```vue
&lt;style scoped&gt;
/* 现有样式保持不变 */
&lt;/style scoped&gt;
```

**关键变更：**
- 导入 `useGameControls` composable
- 创建 `boardRef` 引用游戏板 DOM 元素
- 调用 `useGameControls(boardRef)` 启用控制
- 在模板中将 ref 绑定到根元素

**Row/Col Props 传递：**
- Tile 组件现在接收 row 和 col props
- 这些 props 可用于未来的位置动画（当前计划中暂不使用）

**控制说明：**
- 键盘控制全局生效（window 监听）
- 触摸控制在 boardRef 元素上生效
- 两种输入方式互不冲突，可以同时使用
  </action>
  <verify>
    <automated>
      # 检查 composable 导入和使用
      grep -q "useGameControls" src/components/GameBoard.vue &amp;&amp;
      grep -q "boardRef" src/components/GameBoard.vue &amp;&amp;
      echo "Game controls integrated into GameBoard"
    </automated>
  </verify>
  <done>游戏控制已集成到 GameBoard，键盘和触摸输入都可以控制方块移动</done>
</task>

</tasks>

<verification>
## 整体验证标准

1. **代码验证**
   - [ ] useGameControls.ts composable 存在且格式正确
   - [ ] GameBoard.vue 正确导入和使用 composable
   - [ ] TypeScript 类型检查通过

2. **键盘控制验证**
   - [ ] 方向键可以控制方块移动
   - [ ] WASD 键可以控制方块移动
   - [ ] 按键时页面不滚动（preventDefault）
   - [ ] 游戏结束时不响应键盘输入

3. **触摸控制验证**
   - [ ] 滑动可以控制方块移动
   - [ ] 斜向滑动正确判断主体方向
   - [ ] 滑动阈值合理（不会误触）
   - [ ] 游戏结束时不响应触摸输入

4. **输入优先级验证**
   - [ ] 键盘和触摸可以同时使用
   - [ ] 两种输入方式互不冲突
   - [ ] 事件监听器正确管理（mount/unmount）

5. **边界情况验证**
   - [ ] 游戏结束时不响应输入
   - [ ] 游戏胜利时不响应输入
   - [ ] 无效移动时状态不变
</verification>

<success_criteria>
1. useGameControls.ts composable 创建完成，封装键盘和触摸控制逻辑
2. GameBoard.vue 已更新，集成游戏控制功能
3. 键盘方向键和 WASD 可以控制游戏
4. 触摸滑动可以控制游戏，阈值和方向判断合理
5. 输入优先级正确，键盘和触摸无冲突
6. 为后续计划（游戏状态覆盖层）提供了基础
</success_criteria>

<output>
完成此计划后，创建 `.planning/phases/03-用户界面/03-04-SUMMARY.md`
</output>
