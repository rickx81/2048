---
phase: 02-游戏增强功能
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/stores/game.ts
  - src/core/__tests__/store.test.ts
autonomous: true
requirements:
  - ENHANCE-02
  - ENHANCE-03
  - ENHANCE-04
  - ENHANCE-05

must_haves:
  truths:
    - "游戏结束时自动保存分数到排行榜"
    - "游戏胜利时自动保存分数到排行榜"
    - "页面刷新后自动加载上次的最高分"
    - "页面刷新后自动加载上次的排行榜"
    - "页面刷新后自动恢复游戏进度"
    - "新游戏按钮保留最高分和排行榜，只重置游戏状态"
  artifacts:
    - path: "src/stores/game.ts"
      provides: "完整的游戏状态管理（集成持久化）"
      exports: ["highScore", "leaderboard", "initialize", "reset"]
      contains: "saveGameState, loadGameState"
      min_lines: 180
    - path: "src/core/__tests__/store.test.ts"
      provides: "持久化功能测试"
      min_lines: 450
  key_links:
    - from: "src/stores/game.ts"
      to: "src/core/storage.ts"
      via: "导入所有存储工具"
      pattern: "import.*\\{.*save.*load.*\\}.*from '@/core/storage'"
    - from: "src/stores/game.ts"
      to: "localStorage"
      via: "在游戏结束/胜利/移动时自动保存"
      pattern: "(saveHighScore|saveLeaderboard|saveGameState)\\("
---

<objective>
集成本地存储到 Pinia store，实现游戏进度自动保存和加载，完善新游戏逻辑

目的：提供完整的游戏体验，让玩家的进度和数据能跨会话持久化
输出：完整的游戏状态管理，包含自动保存、加载和新游戏功能
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-游戏增强功能/02-01-SUMMARY.md
@.planning/phases/02-游戏增强功能/02-02-SUMMARY.md

# 已有存储工具（来自 Plan 01）
- saveHighScore/loadHighScore - 最高分管理
- saveLeaderboard/loadLeaderboard/addLeaderboardEntry - 排行榜管理
- saveGameState/loadGameState - 游戏状态管理

# 已有 store 状态（来自 Phase 1 + Plan 02）
- grid, score, status, history
- undoCount, undoLimit, canUndo
- initialize, moveGrid, reset, undo

# 关键需求（来自 CONTEXT.md）
- 游戏结束时自动保存分数到排行榜
- 新游戏按钮保留最高分和排行榜，只重置游戏状态
- 刷新页面自动恢复上次游戏状态
- 刷新页面自动加载最高分和排行榜
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 集成本地存储到 store</name>
  <files>src/stores/game.ts, src/core/__tests__/store.test.ts</files>
  <behavior>
    - 测试 1: store 创建时自动加载最高分
    - 测试 2: store 创建时自动加载排行榜
    - 测试 3: store 创建时尝试恢复游戏状态（如果有）
    - 测试 4: 游戏结束时自动保存分数到排行榜
    - 测试 5: 游戏胜利时自动保存分数到排行榜
    - 测试 6: 每次移动后自动保存游戏状态
    - 测试 7: 分数超过最高分时自动更新最高分
    - 测试 8: 新游戏按钮重置游戏状态但保留最高分和排行榜
    - 测试 9: 排行榜自动保持前 10 名
  </behavior>
  <action>
修改 src/stores/game.ts，集成本地存储：

1. **导入存储工具**
```typescript
import {
  saveHighScore,
  loadHighScore,
  saveLeaderboard,
  loadLeaderboard,
  addLeaderboardEntry,
  saveGameState,
  loadGameState
} from '@/core/storage'
import type { LeaderboardEntry } from '@/core/storage'
```

2. **新增状态**
```typescript
/** 最高分 */
const highScore = ref<number>(0)

/** 排行榜（前 10 名） */
const leaderboard = ref<LeaderboardEntry[]>([])
```

3. **在 store 创建时初始化持久化数据**
```typescript
// 在 return 之前添加初始化逻辑
// 加载最高分
highScore.value = loadHighScore()

// 加载排行榜
leaderboard.value = loadLeaderboard()

// 尝试恢复游戏状态
const savedState = loadGameState()
if (savedState) {
  grid.value = savedState.grid
  score.value = savedState.score
  status.value = savedState.status
  // 不恢复 history 和 undoCount，避免撤销混乱
}
```

4. **修改 moveGrid 方法**
- 在更新分数后，检查是否超过最高分
- 如果超过，更新最高分并保存
- 在最后保存游戏状态

```typescript
// 更新分数
score.value += result.score

// 更新最高分
if (score.value > highScore.value) {
  highScore.value = score.value
  saveHighScore(highScore.value)
}

// 更新游戏状态后保存游戏进度
// ... 状态更新逻辑
saveGameState({ grid: grid.value, score: score.value, status: status.value })
```

5. **新增保存分数到排行榜的逻辑**
```typescript
function saveScoreToLeaderboard() {
  addLeaderboardEntry(score.value)
  leaderboard.value = loadLeaderboard() // 重新加载以获取更新后的数据
}
```

6. **修改 moveGrid 中的游戏状态检测**
- 当游戏变为 LOST 或 WON 时，调用 saveScoreToLeaderboard()

```typescript
// 更新游戏状态
if (checkGameWon(grid.value) && status.value !== GameStatus.WON) {
  status.value = GameStatus.WON
  saveScoreToLeaderboard()
} else if (checkGameOver(grid.value)) {
  status.value = GameStatus.LOST
  saveScoreToLeaderboard()
}
```

7. **修改 reset 方法**
- 只重置游戏状态，不清空最高分和排行榜

```typescript
function reset() {
  grid.value = createEmptyGrid()
  score.value = 0
  status.value = GameStatus.IDLE
  history.value = []
  undoCount.value = 0
  scoreHistory.value = []
  // 不清空 highScore 和 leaderboard
  // 保存清空后的状态（或删除游戏状态）
  saveGameState({ grid: grid.value, score: score.value, status: status.value })
}
```

8. **修改 initialize 方法**
- 调用 reset() 重置游戏状态
- 保留 highScore 和 leaderboard

```typescript
function initialize() {
  reset()
  grid.value = createInitialGrid()
  score.value = 0
  status.value = GameStatus.PLAYING
  saveGameState({ grid: grid.value, score: score.value, status: status.value })
}
```

9. **导出新增的状态**
- highScore, leaderboard

实现要求：
- 保持与现有代码风格一致
- 添加详细的中文注释
- 确保存储操作不会阻塞游戏流程
- 处理存储失败的边界情况（存储工具已处理）
  </action>
  <verify>
    <automated>npm test -- store.test.ts</automated>
  </verify>
  <done>
  - 所有 9+ 个测试用例通过
  - 游戏结束/胜利时自动保存分数到排行榜
  - 最高分自动更新和保存
  - 页面刷新后自动恢复游戏状态
  - 新游戏按钮正确保留最高分和排行榜
  - 排行榜保持前 10 名
  </done>
</task>

</tasks>

<verification>
- 运行完整测试套件验证持久化功能
- 手动测试完整流程：游戏 → 结束 → 刷新 → 恢复
- 验证排行榜正常工作
- 确认新游戏按钮保留持久化数据
</verification>

<success_criteria>
- 本地存储完全集成到 store
- 游戏结束/胜利时自动保存分数
- 页面刷新后自动恢复游戏状态
- 最高分和排行榜跨会话持久化
- 新游戏功能正确实现
</success_criteria>

<output>
After completion, create `.planning/phases/02-游戏增强功能/02-03-SUMMARY.md`
</output>
