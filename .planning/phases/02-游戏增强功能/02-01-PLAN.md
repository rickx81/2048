---
phase: 02-游戏增强功能
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/storage.ts
  - src/core/__tests__/storage.test.ts
autonomous: true
requirements:
  - ENHANCE-03
  - ENHANCE-04

must_haves:
  truths:
    - "最高分在页面刷新后保持不变"
    - "排行榜能保存和加载前 10 名高分记录"
    - "游戏进度在刷新后能恢复到之前的状态"
    - "localStorage 不可用时代码能优雅降级"
  artifacts:
    - path: "src/core/storage.ts"
      provides: "localStorage 封装工具"
      exports: ["saveHighScore", "loadHighScore", "saveLeaderboard", "loadLeaderboard", "saveGameState", "loadGameState"]
      min_lines: 80
    - path: "src/core/__tests__/storage.test.ts"
      provides: "存储功能测试覆盖"
      min_lines: 150
  key_links:
    - from: "src/core/storage.ts"
      to: "window.localStorage"
      via: "localStorage.setItem/getItem"
      pattern: "localStorage\\.(setItem|getItem)"
    - from: "src/core/storage.ts"
      to: "src/core/types.ts"
      via: "导入 GameState 类型"
      pattern: "import.*GameState"
---

<objective>
创建本地存储工具模块，实现最高分、排行榜和游戏进度的持久化存储

目的：为游戏提供跨会话的数据持久化能力，让玩家刷新页面后不丢失进度
输出：完整的 localStorage 封装工具，包含测试覆盖
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 存储键名约定（来自 CONTEXT.md）
__GAME_2048_HIGHSCORE__ - 最高分
__GAME_2048_LEADERBOARD__ - 排行榜
__GAME_2048_GAME_STATE__ - 游戏进度

# 已有类型定义（来自 src/core/types.ts）
```typescript
export interface GameState {
  grid: Grid;
  score: number;
  status: GameStatus;
}
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建本地存储工具模块</name>
  <files>src/core/storage.ts, src/core/__tests__/storage.test.ts</files>
  <behavior>
    - 测试 1: saveHighScore 和 loadHighScore 能正确保存和加载最高分
    - 测试 2: loadHighScore 在没有数据时返回 0
    - 测试 3: saveLeaderboard 和 loadLeaderboard 能保存和加载排行榜数据
    - 测试 4: loadLeaderboard 在没有数据时返回空数组
    - 测试 5: saveGameState 和 loadGameState 能保存和恢复完整游戏状态
    - 测试 6: loadGameState 在没有数据时返回 null
    - 测试 7: localStorage 不可用时，所有 load 方法返回默认值且不抛出错误
    - 测试 8: JSON 解析失败时返回默认值且不抛出错误
  </behavior>
  <action>
创建 src/core/storage.ts，实现以下纯函数：

1. **常量定义**
   - STORAGE_KEY_PREFIX = '__GAME_2048_'
   - HIGHSCORE_KEY = `${STORAGE_KEY_PREFIX}HIGHSCORE__`
   - LEADERBOARD_KEY = `${STORAGE_KEY_PREFIX}LEADERBOARD__`
   - GAME_STATE_KEY = `${STORAGE_KEY_PREFIX}GAME_STATE__`

2. **排行榜类型定义**
   ```typescript
   export interface LeaderboardEntry {
     score: number;
     timestamp: number; // Unix 时间戳
   }
   ```

3. **最高分管理**
   - saveHighScore(score: number): void - 保存最高分到 localStorage
   - loadHighScore(): number - 加载最高分，默认返回 0

4. **排行榜管理**
   - saveLeaderboard(entries: LeaderboardEntry[]): void - 保存排行榜（前 10 名）
   - loadLeaderboard(): LeaderboardEntry[] - 加载排行榜，默认返回空数组
   - addLeaderboardEntry(score: number): void - 辅助函数：添加新分数并保持前 10 名
     - 按 score 降序排序
     - 同分时新记录排在前面（更大的 timestamp）
     - 只保留前 10 条记录

5. **游戏状态管理**
   - saveGameState(state: GameState): void - 保存游戏进度（序列化整个状态）
   - loadGameState(): GameState | null - 加载游戏进度，无数据返回 null

6. **错误处理**
   - 所有 localStorage 操作包裹在 try-catch 中
   - localStorage 不可用时（如隐私模式），静默失败，返回默认值
   - JSON.parse 失败时，返回默认值

实现要求：
- 使用 JSON.stringify/JSON.parse 序列化数据
- 导出所有公共函数和类型
- 添加详细的中文注释
- 不使用 console.error（静默失败）
  </action>
  <verify>
    <automated>npm test -- storage.test.ts</automated>
  </verify>
  <done>
  - 所有 8+ 个测试用例通过
  - localStorage 封装工具模块完成
  - 导出所有必要的函数和类型
  - 错误处理健壮（localStorage 不可用时不崩溃）
  </done>
</task>

</tasks>

<verification>
- 运行测试套件验证所有存储功能正常
- 检查代码覆盖率，确保所有分支都被测试
- 验证 localStorage 键名符合约定
</verification>

<success_criteria>
- localStorage 工具模块创建完成
- 最高分、排行榜、游戏状态的保存和加载功能正常
- 错误处理健壮，localStorage 不可用时不影响游戏运行
- 测试覆盖率达到 100%
</success_criteria>

<output>
After completion, create `.planning/phases/02-游戏增强功能/02-01-SUMMARY.md`
</output>
