---
phase: 08-依赖升级
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
autonomous: false
requirements:
  - DEPS-01
  - DEPS-02
  - DEPS-03
  - DEPS-04
  - DEPS-05

must_haves:
  truths:
    - "所有核心依赖版本已更新到最新稳定版"
    - "pnpm-lock.yaml 已更新并锁定正确的版本"
    - "升级分支已创建并与主分支隔离"
  artifacts:
    - path: "package.json"
      provides: "依赖版本定义"
      min_lines: 50
    - path: "pnpm-lock.yaml"
      provides: "锁定版本解析"
      contains: "vite: 8.0.3"
    - path: ".git/refs/heads/deps/vite-8-upgrade"
      provides: "升级分支隔离"
  key_links:
    - from: "package.json"
      to: "pnpm-lock.yaml"
      via: "pnpm install"
      pattern: "pnpm install 解析并锁定依赖版本"

---

<objective>
在隔离的分支中升级所有核心依赖到最新稳定版本，为后续验证做准备。

目的：在保护主分支稳定性的前提下，探索 Vite 8 和相关依赖的升级可行性。
输出：创建 `deps/vite-8-upgrade` 分支，更新 package.json 和 pnpm-lock.yaml。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-依赖升级/08-CONTEXT.md
@.planning/phases/08-依赖升级/08-RESEARCH.md
@.planning/REQUIREMENTS.md
@package.json
@vite.config.ts
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>任务 1: 创建升级分支并检查当前状态</name>
  <files>
    .git/refs/heads/deps/vite-8-upgrade
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/package.json（当前依赖版本）
    - D:/Projects/demo/games/2048/.planning/phases/08-依赖升级/08-RESEARCH.md（目标版本清单）
  </read_first>
  <action>
    从 master 分支创建新的特性分支 `deps/vite-8-upgrade`，确保所有本地更改已提交或暂存：

    1. 检查当前分支状态：
       ```bash
       git status
       ```

    2. 切换到 master 分支并拉取最新代码：
       ```bash
       git checkout master
       git pull origin master
       ```

    3. 创建并切换到升级分支：
       ```bash
       git checkout -b deps/vite-8-upgrade
       ```

    4. 验证分支创建成功：
       ```bash
       git branch --show-current
       ```
       输出应为 `deps/vite-8-upgrade`。
  </action>
  <verify>
    <automated>git branch --show-current | grep -q "deps/vite-8-upgrade" && echo "分支创建成功" || echo "分支创建失败"</automated>
  </verify>
  <done>
    - 当前分支为 `deps/vite-8-upgrade`
    - 分支基于最新的 master 分支
    - 工作目录干净（无未提交更改）
  </done>
</task>

<task type="auto">
  <name>任务 2: 升级核心生产依赖</name>
  <files>
    package.json
    pnpm-lock.yaml
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/package.json（当前版本）
    - D:/Projects/demo/games/2048/.planning/phases/08-依赖升级/08-RESEARCH.md（研究文档中的目标版本）
  </read_first>
  <action>
    根据 RESEARCH.md 中的标准栈，升级以下核心生产依赖：

    1. 升级 Vue 到 3.5.31：
       ```bash
       pnpm add vue@^3.5.31
       ```

    2. 升级 VueUse 到 14.2.1（已是最新版本，无需升级，但命令可验证）：
       ```bash
       pnpm add @vueuse/core@^14.2.1
       ```

    3. 升级 Pinia 到 3.0.4（已是最新版本，无需升级，但命令可验证）：
       ```bash
       pnpm add pinia@^3.0.4
       ```

    4. 升级 TailwindCSS 到 4.2.2：
       ```bash
       pnpm add tailwindcss@^4.2.2
       ```

    注意：根据 RESEARCH.md，Pinia 和 VueUse 已经是最新版本，命令运行后会保持不变。这是预期行为。
  </action>
  <verify>
    <automated>grep '"vue": "3.5' D:/Projects/demo/games/2048/package.json && grep '"tailwindcss": "4.2.2' D:/Projects/demo/games/2048/package.json</automated>
  </verify>
  <done>
    - package.json 中 vue 版本为 ^3.5.31
    - package.json 中 tailwindcss 版本为 ^4.2.2
    - pnpm-lock.yaml 已更新以锁定确切版本
  </done>
</task>

<task type="auto">
  <name>任务 3: 升级核心开发依赖</name>
  <files>
    package.json
    pnpm-lock.yaml
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/package.json（当前 devDependencies 版本）
    - D:/Projects/demo/games/2048/.planning/phases/08-依赖升级/08-RESEARCH.md（标准栈章节的目标版本）
  </read_first>
  <action>
    根据 RESEARCH.md 中的标准栈，升级以下核心开发依赖：

    1. 升级 Vite 到 8.0.3（这是本次升级的核心）：
       ```bash
       pnpm add -D vite@^8.0.3
       ```

    2. 升级 TypeScript 到 6.0.2：
       ```bash
       pnpm add -D typescript@^6.0.2
       ```

    3. 升级 Vitest 到 4.1.2：
       ```bash
       pnpm add -D vitest@^4.1.2
       ```

    4. 升级 @vitejs/plugin-vue 到 6.0.5：
       ```bash
       pnpm add -D @vitejs/plugin-vue@^6.0.5
       ```

    5. 升级 vue-tsc 到 3.2.6：
       ```bash
       pnpm add -D vue-tsc@^3.2.6
       ```

    这些升级需要按顺序执行，确保依赖关系正确解析。
  </action>
  <verify>
    <automated>grep '"vite": "8.0.3' D:/Projects/demo/games/2048/package.json && grep '"typescript": "6.0.2' D:/Projects/demo/games/2048/package.json && grep '"vitest": "4.1.2' D:/Projects/demo/games/2048/package.json</automated>
  </verify>
  <done>
    - package.json 中 vite 版本为 ^8.0.3
    - package.json 中 typescript 版本为 ^6.0.2
    - package.json 中 vitest 版本为 ^4.1.2
    - package.json 中 @vitejs/plugin-vue 版本为 ^6.0.5
    - package.json 中 vue-tsc 版本为 ^3.2.6
    - pnpm-lock.yaml 已更新
  </done>
</task>

<task type="auto">
  <name>任务 4: 升级支持性开发依赖</name>
  <files>
    package.json
    pnpm-lock.yaml
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/package.json（当前 devDependencies 版本）
    - D:/Projects/demo/games/2048/.planning/phases/08-依赖升级/08-RESEARCH.md（支持性依赖章节）
  </read_first>
  <action>
    根据 RESEARCH.md，升级剩余的支持性开发依赖：

    1. 升级 @vitejs/plugin-vue-jsx 到 5.1.5：
       ```bash
       pnpm add -D @vitejs/plugin-vue-jsx@^5.1.5
       ```

    2. 升级 vite-plugin-vue-devtools 到 8.1.1：
       ```bash
       pnpm add -D vite-plugin-vue-devtools@^8.1.1
       ```

    3. 升级 ESLint 到 10.1.0：
       ```bash
       pnpm add -D eslint@^10.1.0
       ```

    这些依赖虽然不是核心，但与 Vite 8 的兼容性相关。
  </action>
  <verify>
    <automated>grep '"@vitejs/plugin-vue-jsx": "5.1.5' D:/Projects/demo/games/2048/package.json && grep '"vite-plugin-vue-devtools": "8.1.1' D:/Projects/demo/games/2048/package.json && grep '"eslint": "10.1.0' D:/Projects/demo/games/2048/package.json</automated>
  </verify>
  <done>
    - package.json 中 @vitejs/plugin-vue-jsx 版本为 ^5.1.5
    - package.json 中 vite-plugin-vue-devtools 版本为 ^8.1.1
    - package.json 中 eslint 版本为 ^10.1.0
    - 所有支持性依赖版本已更新
  </done>
</task>

<task type="auto">
  <name>任务 5: 重新安装依赖并验证锁定文件</name>
  <files>
    pnpm-lock.yaml
    node_modules/
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/package.json（已更新的版本）
    - D:/Projects/demo/games/2048/pnpm-lock.yaml（当前锁定文件）
  </read_first>
  <action>
    清理并重新安装所有依赖，确保 pnpm-lock.yaml 完全更新且一致：

    1. 删除现有的 node_modules 和锁定文件：
       ```bash
       rm -rf node_modules pnpm-lock.yaml
       ```

    2. 重新安装所有依赖：
       ```bash
       pnpm install
       ```

    3. 验证关键依赖版本已正确锁定：
       ```bash
       grep -A 2 '"vite@"' pnpm-lock.yaml | head -5
       grep -A 2 '"vue@"' pnpm-lock.yaml | head -5
       grep -A 2 '"typescript@"' pnpm-lock.yaml | head -5
       ```

    4. 检查是否有版本冲突或解析错误：
       ```bash
       pnpm list --depth=0 | grep -E "vite|vue|typescript|vitest"
       ```
  </action>
  <verify>
    <automated>grep "vite@8.0.3" D:/Projects/demo/games/2048/pnpm-lock.yaml && grep "vue@3.5.31" D:/Projects/demo/games/2048/pnpm-lock.yaml && pnpm list vite --depth=0 | grep "8.0.3"</automated>
  </verify>
  <done>
    - node_modules 已完全重新安装
    - pnpm-lock.yaml 已更新且包含正确的锁定版本
    - pnpm list 显示所有关键依赖版本正确
    - 无版本冲突或解析错误
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    完成了依赖升级的准备工作：
    1. 创建了隔离的升级分支 `deps/vite-8-upgrade`
    2. 升级了所有核心依赖（Vue 3.5.31、Vite 8.0.3、TypeScript 6.0.2 等）
    3. 升级了所有支持性开发依赖
    4. 重新安装了依赖并更新了锁定文件
  </what-built>
  <how-to-verify>
    请验证以下内容：

    1. 检查当前分支：
       ```bash
       git branch --show-current
       ```
       应显示 `deps/vite-8-upgrade`

    2. 检查 package.json 中关键依赖版本：
       ```bash
       grep -E '"(vite|vue|typescript|vitest)":' package.json
       ```
       应显示：
       - "vite": "^8.0.3"
       - "vue": "^3.5.31"
       - "typescript": "^6.0.2"
       - "vitest": "^4.1.2"

    3. 检查锁定文件中的确切版本：
       ```bash
       grep "vite@" pnpm-lock.yaml | head -3
       ```
       应包含 `vite@8.0.3`

    4. 验证依赖已安装：
       ```bash
       pnpm list --depth=0 | grep -E "vite|vue|typescript|vitest"
       ```
       应显示所有依赖的正确版本
  </how-to-verify>
  <resume-signal>
    输入 "approved" 继续到下一个计划（验证阶段），或描述问题以中止。
  </resume-signal>
</task>

</tasks>

<verification>
在完成本计划后，执行以下验证：

1. **分支隔离验证**：
   ```bash
   git branch --show-current
   ```
   输出应为 `deps/vite-8-upgrade`

2. **依赖版本验证**：
   ```bash
   grep -E '"(vite|vue|typescript|vitest|@vitejs/plugin-vue|vue-tsc)":' package.json
   ```
   所有版本号应与 RESEARCH.md 中的标准栈一致

3. **锁定文件验证**：
   ```bash
   grep "vite@8.0.3" pnpm-lock.yaml
   grep "vue@3.5.31" pnpm-lock.yaml
   ```
   应找到匹配项

4. **依赖安装验证**：
   ```bash
   pnpm list vite vue typescript vitest --depth=0
   ```
   应显示正确的已安装版本
</verification>

<success_criteria>
- [x] 创建了 `deps/vite-8-upgrade` 分支
- [x] package.json 中所有核心依赖版本已更新
- [x] pnpm-lock.yaml 已更新并锁定正确版本
- [x] node_modules 已重新安装
- [x] 人工验证确认分支和版本正确
</success_criteria>

<output>
完成本计划后，创建 `.planning/phases/08-依赖升级/08-01-SUMMARY.md`，记录：
1. 升级的依赖清单及版本变更
2. pnpm-lock.yaml 的主要变更摘要
3. 遇到的问题（如有）和解决方案
4. 验证命令和输出结果
</output>
