---
phase: 06-性能优化
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/06-性能优化/06-01-SUMMARY.md]
autonomous: true
requirements: [PERF-01, PERF-02]
user_setup: []

must_haves:
  truths:
    - "Chrome DevTools Performance 录制完成，获得优化前基线数据"
    - "现有动画实现已审查，识别性能瓶颈"
    - "确定哪些动画需要优化（触发布局的属性）"
  artifacts:
    - path: ".planning/phases/06-性能优化/06-01-SUMMARY.md"
      provides: "性能基线报告和优化建议清单"
      contains: "FPS 数据、Layout/Paint 时间、优化建议"
  key_links: []
---

<objective>
录制性能基线数据并审查现有动画实现，识别需要优化的性能瓶颈

Purpose: 在进行优化前建立性能基线，量化当前动画表现，并明确哪些动画实现需要改进

Output: 性能基线报告（FPS、Layout/Paint 时间）+ 动画实现审查清单
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-性能优化/06-RESEARCH.md
@src/components/Tile.vue
@src/components/App.vue
@src/components/GameBoard.vue
</context>

<tasks>

<task type="auto">
  <name>任务 1: 使用 Chrome DevTools 录制性能基线</name>
  <files>.planning/phases/06-性能优化/06-01-SUMMARY.md</files>
  <action>
    创建性能基线报告文档，包含以下内容：

    1. **录制步骤**：
       - 打开 Chrome DevTools (F12)
       - 切换到 Performance 面板
       - 点击 Record
       - 执行游戏操作（移动方块 10-15 次）
       - 停止录制

    2. **记录数据**：
       - 平均 FPS（目标：稳定 60fps）
       - FPS 波动范围（是否有掉帧）
       - 每帧渲染时间（应 < 16.67ms）
       - Long Tasks（是否有 > 50ms 的任务）
       - Layout 和 Paint 时间占比
       - 合成层数量（Layers 面板）

    3. **截图保存**：
       - FPS 图表截图
       - Main 线程火焰图
       - Frames 详细视图

    创建 SUMMARY.md 文档记录所有基线数据
  </action>
  <verify>
    <automated>检查 .planning/phases/06-性能优化/06-01-SUMMARY.md 文件存在并包含性能基线数据</automated>
  </verify>
  <done>性能基线报告已创建，包含 FPS、Layout/Paint 时间和截图</done>
</task>

<task type="auto">
  <name>任务 2: 审查现有动画实现</name>
  <files>.planning/phases/06-性能优化/06-01-SUMMARY.md</files>
  <action>
    审查以下文件中的动画实现：

    1. **src/App.vue**（全局动画定义）：
       - 检查 @keyframes pop-in、pulse-merge、slide-move
       - 确认是否使用 transform 和 opacity
       - 识别是否使用了触发布局的属性（width/height/left/top）

    2. **src/components/Tile.vue**（方块组件）：
       - 检查 .will-change-transform 类的使用
       - 确认 will-change 是否动态管理（动画前添加，结束后移除）
       - 检查是否有硬编码的 will-change（永久设置）

    3. **src/components/GameBoard.vue**（游戏板）：
       - 检查是否有不必要的布局计算
       - 确认 grid 布局是否高效

    在 SUMMARY.md 中记录：
    - 当前使用的 CSS 属性（✓ GPU 加速 / ✗ 触发布局）
    - will-change 使用情况（动态 / 永久 / 无）
    - 发现的性能问题
    - 优化建议清单
  </action>
  <verify>
    <automated>检查 SUMMARY.md 包含动画审查结果和优化建议</automated>
  </verify>
  <done>动画审查完成，优化建议清单已记录</done>
</task>

</tasks>

<verification>
- [ ] 性能基线报告包含 FPS、Layout/Paint 时间数据
- [ ] 动画审查完成，识别出需要优化的部分
- [ ] 优化建议清单明确具体
</verification>

<success_criteria>
- 性能基线数据完整（FPS、渲染时间、截图）
- 现有动画实现问题已识别
- 优化方向明确（哪些属性需要替换为 transform/opacity）
</success_criteria>

<output>
完成后创建 `.planning/phases/06-性能优化/06-01-SUMMARY.md` 包含基线数据和审查结果
</output>
