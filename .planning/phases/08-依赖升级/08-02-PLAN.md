---
phase: 08-依赖升级
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - dist/
autonomous: true
requirements:
  - DEPS-06
  - DEPS-07

must_haves:
  truths:
    - "所有类型检查通过（无 TypeScript 错误）"
    - "生产构建成功生成 dist 目录"
    - "所有 111 个单元测试通过"
    - "所有 E2E 测试通过（如果存在）"
  artifacts:
    - path: "dist/index.html"
      provides: "生产构建输出"
      min_lines: 1
    - path: "node_modules/.tmp/"
      provides: "TypeScript 构建缓存"
      contains: "tsbuildinfo"
    - path: "test-results/"
      provides: "测试结果"
      contains: "results.xml"
  key_links:
    - from: "vite.config.ts"
      to: "dist/"
      via: "pnpm build"
      pattern: "Vite 8 Rolldown 打包"
    - from: "src/core/__tests__/*.test.ts"
      to: "test-results/"
      via: "vitest"
      pattern: "Vitest 4.1.2 运行测试"

---

<objective>
使用分层渐进验证策略，从快到慢验证依赖升级后的系统完整性。

目的：在最小化时间成本的前提下，快速发现并定位升级引入的问题。
输出：验证报告，包括类型检查、构建、单元测试和 E2E 测试的结果。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-依赖升级/08-CONTEXT.md
@.planning/phases/08-依赖升级/08-RESEARCH.md
@.planning/phases/08-依赖升级/08-01-SUMMARY.md
@.planning/REQUIREMENTS.md
@vite.config.ts
@tsconfig.json
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>任务 1: 第 1 层 - TypeScript 类型检查</name>
  <files>
    node_modules/.tmp/
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/package.json（typescript 版本 6.0.2）
    - D:/Projects/demo/games/2048/tsconfig.json（TypeScript 配置）
    - D:/Projects/demo/games/2048/tsconfig.app.json（应用配置）
    - D:/Projects/demo/games/2048/tsconfig.vitest.json（测试配置）
    - D:/Projects/demo/games/2048/.planning/phases/08-依赖升级/08-RESEARCH.md（TypeScript 6.0 迁移注意事项）
  </read_first>
  <action>
    运行 TypeScript 类型检查，验证 TypeScript 6.0.2 与现有代码的兼容性：

    1. 清理 TypeScript 构建缓存（如有）：
       ```bash
       rm -rf node_modules/.tmp
       ```

    2. 运行完整类型检查：
       ```bash
       pnpm type-check
       ```

    3. 如果出现类型错误，记录错误数量和位置：
       ```bash
       pnpm type-check 2>&1 | tee type-check.log
       ```

    根据 RESEARCH.md，TypeScript 6.0 引入了更严格的类型检查，可能会暴露旧版本的类型问题。如果错误数量少于 10 个且集中在第三方库类型定义中，可以继续验证；如果错误数量超过 20 个或影响核心逻辑，需要评估是否回退。
  </action>
  <verify>
    <automated>pnpm type-check 2>&1 | grep -q "TS0" && echo "类型检查失败" || echo "类型检查通过"</automated>
  </verify>
  <done>
    - pnpm type-check 退出码为 0（无类型错误）
    - 或类型错误可接受（< 10 个，非核心逻辑）
    - 如有类型错误，已记录在 type-check.log
  </done>
</task>

<task type="auto">
  <name>任务 2: 第 2 层 - 生产构建验证</name>
  <files>
    dist/
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/vite.config.ts（Vite 配置）
    - D:/Projects/demo/games/2048/.planning/phases/08-依赖升级/08-RESEARCH.md（Vite 8 Rolldown 兼容性）
  </read_first>
  <action>
    运行生产构建，验证 Vite 8.0.3 的 Rolldown 打包工具与项目配置的兼容性：

    1. 清理旧的构建输出：
       ```bash
       rm -rf dist
       ```

    2. 运行生产构建（包含类型检查）：
       ```bash
       pnpm build
       ```

    3. 检查构建输出：
       ```bash
       ls -lh dist/
       du -sh dist/
       ```

    4. 验证关键文件存在：
       ```bash
       ls dist/index.html
       ls dist/assets/*.js | head -5
       ```

    根据 RESEARCH.md，Vite 8 使用 Rolldown 替代 Rollup，虽然提供兼容层，但可能在边缘情况下行为不同。如果构建失败，需要检查是否与 esbuild 配置或 rollupOptions 有关。

    注意构建时间变化：Vite 8 的 Rolldown 应该显著提升生产构建速度（10-30x），记录构建时间以供参考。
  </action>
  <verify>
    <automated>test -f "D:/Projects/demo/games/2048/dist/index.html" && echo "构建成功" || echo "构建失败"</automated>
  </verify>
  <done>
    - dist/index.html 文件存在
    - dist/assets/ 目录包含 JS 和 CSS 文件
    - 构建退出码为 0（无错误）
    - 构建时间已记录（用于对比 Vite 7 vs Vite 8）
  </done>
</task>

<task type="auto">
  <name>任务 3: 第 3 层 - 快速核心测试</name>
  <files>
    test-results/
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/src/core/__tests__/game.test.ts（核心游戏逻辑测试）
    - D:/Projects/demo/games/2048/vitest.config.ts（Vitest 配置）
  </read_first>
  <action>
    运行单个核心测试文件，快速验证升级对核心逻辑的影响：

    1. 运行游戏逻辑测试（最核心的测试文件）：
       ```bash
       pnpm test:unit src/core/__tests__/game.test.ts
       ```

    2. 检查测试结果摘要：
       ```bash
       pnpm test:unit src/core/__tests__/game.test.ts --reporter=verbose
       ```

    3. 如果测试失败，记录失败用例：
       ```bash
       pnpm test:unit src/core/__tests__/game.test.ts 2>&1 | tee test-results-core.log
       ```

    这个步骤可以快速发现升级是否破坏了核心逻辑，而不需要运行全部 111 个测试。核心游戏逻辑测试覆盖了移动、合并、游戏状态检测等关键功能，是升级影响的敏感指标。
  </action>
  <verify>
    <automated>pnpm test:unit src/core/__tests__/game.test.ts --run 2>&1 | grep -E "Test Files|Tests" | grep -v "0 "</automated>
  </verify>
  <done>
    - 核心游戏逻辑测试全部通过
    - 测试报告中显示 "Test Files 1 passed" 或类似
    - 无失败的测试用例
    - 如有失败，已记录在 test-results-core.log
  </done>
</task>

<task type="auto">
  <name>任务 4: 第 4 层 - 完整单元测试验证</name>
  <files>
    test-results/
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/.planning/STATE.md（当前有 111 个单元测试）
    - D:/Projects/demo/games/2048/vitest.config.ts
  </read_first>
  <action>
    运行所有单元测试，验证升级对整个测试套件的影响：

    1. 运行完整单元测试套件：
       ```bash
       pnpm test:unit --run
       ```

    2. 检查测试覆盖和结果：
       ```bash
       pnpm test:unit --run --reporter=verbose 2>&1 | tee test-results-full.log
       ```

    3. 验证测试数量：
       ```bash
       grep "Test Files" test-results-full.log
       grep "Tests" test-results-full.log
       ```

    根据 STATE.md，项目有 111 个单元测试。升级后应该保持相同数量的测试通过。如果测试失败，区分以下情况：
    - 核心逻辑测试失败 → 可能是 TypeScript 6.0 或 Vitest 4.1.2 的行为变化
    - 组件测试失败 → 可能是 Vue 3.5.31 或 @vue/test-utils 的变化
    - Store 测试失败 → 可能是 Pinia 类型定义的变化

    如果失败测试数量 < 5 个且为边缘情况，可以标记为技术债务继续；如果失败测试数量 > 10 个或涉及核心功能，需要评估回退。
  </action>
  <verify>
    <automated>pnpm test:unit --run 2>&1 | grep -E "Test Files.*[0-9]+ passed" | grep -v "0 "</automated>
  </verify>
  <done>
    - 所有 111 个单元测试通过
    - 测试报告中显示 "Test Files XX passed (XX)"
    - 无失败的测试用例
    - 如有失败，已记录并分类在 test-results-full.log
  </done>
</task>

<task type="auto">
  <name>任务 5: 第 5 层 - E2E 测试验证</name>
  <files>
    test-results/
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/playwright.config.ts（Playwright 配置）
    - D:/Projects/demo/games/2048/e2e/（E2E 测试目录，如存在）
  </read_first>
  <action>
    运行 E2E 测试，验证升级对端到端流程的影响：

    1. 首先检查 E2E 测试文件是否存在：
       ```bash
       ls e2e/*.spec.ts 2>/dev/null | wc -l
       ```

    2. 如果 E2E 测试存在，运行完整 E2E 套件：
       ```bash
       pnpm test:e2e
       ```

    3. 如果 E2E 测试不存在，记录此情况并跳过：
       ```bash
       echo "无 E2E 测试文件，跳过 E2E 验证" | tee test-results-e2e.log
       ```

    根据 ROADMAP.md 和 REQUIREMENTS.md，E2E 测试是 Phase 9 的目标，Phase 8 可能还没有 E2E 测试。如果没有 E2E 测试，这不构成升级失败的判定标准。如果有 E2E 测试，必须全部通过才能继续。
  </action>
  <verify>
    <automated>if [ $(ls e2e/*.spec.ts 2>/dev/null | wc -l) -gt 0 ]; then pnpm test:e2e 2>&1 | grep -E "passed"; else echo "无 E2E 测试"; fi</automated>
  </verify>
  <done>
    - 如果 E2E 测试存在，所有测试通过
    - 如果 E2E 测试不存在，已记录此情况
    - 无 E2E 测试失败导致阻断性问题
  </done>
</task>

<task type="auto">
  <name>任务 6: 开发服务器快速验证</name>
  <files>
    - 临时进程
  </files>
  <read_first>
    - D:/Projects/demo/games/2048/vite.config.ts
    - D:/Projects/demo/games/2048/.planning/phases/08-依赖升级/08-RESEARCH.md（Vite 8 开发服务器变化）
  </read_first>
  <action>
    启动开发服务器并验证基本功能，确保 Vite 8 在开发模式下正常工作：

    1. 启动开发服务器（后台运行）：
       ```bash
       timeout 30 pnpm dev 2>&1 | tee dev-server.log &
       DEV_PID=$!
       ```

    2. 等待服务器启动（最多 30 秒）：
       ```bash
       sleep 10
       ```

    3. 检查服务器是否成功启动：
       ```bash
       grep -E "Local:|Network:" dev-server.log
       ```

    4. 测试 HMR（热模块替换）是否工作：
       - 暂时不需要验证 HMR（复杂度高），只需确认服务器能启动

    5. 停止开发服务器：
       ```bash
       kill $DEV_PID 2>/dev/null || true
       ```

    Vite 8 的开发服务器应该与 Vite 7 行为一致。如果服务器无法启动，检查端口占用或配置问题。
  </action>
  <verify>
    <automated>grep -E "Local:.*http://localhost" dev-server.log 2>/dev/null && echo "开发服务器启动成功" || echo "开发服务器启动失败"</automated>
  </verify>
  <done>
    - 开发服务器成功启动并监听端口
    - 日志中显示 "Local:" 和 "Network:" 地址
    - 无启动错误或异常崩溃
    - 开发服务器日志已保存到 dev-server.log
  </done>
</task>

</tasks>

<verification>
完成本计划后，执行以下验证：

1. **类型检查验证**：
   ```bash
   cat type-check.log 2>/dev/null | grep -E "error TS" | wc -l
   ```
   应显示 0 或可接受的错误数量

2. **构建输出验证**：
   ```bash
   ls -lh dist/ | head -10
   ```
   应显示 index.html 和 assets 文件

3. **测试结果验证**：
   ```bash
   grep "Test Files" test-results-full.log
   grep "Tests" test-results-full.log
   ```
   应显示全部通过

4. **开发服务器验证**：
   ```bash
   grep "Local:" dev-server.log
   ```
   应显示本地地址
</verification>

<success_criteria>
- [x] TypeScript 类型检查通过（0 错误或可接受数量）
- [x] 生产构建成功，dist 目录存在
- [x] 核心游戏逻辑测试通过
- [x] 所有 111 个单元测试通过
- [x] E2E 测试通过（如果存在）
- [x] 开发服务器能正常启动
</success_criteria>

<output>
完成本计划后，创建 `.planning/phases/08-依赖升级/08-02-SUMMARY.md`，记录：
1. 每层验证的结果（类型检查、构建、核心测试、完整测试、E2E、开发服务器）
2. 遇到的问题和解决方案
3. 性能对比（如构建时间）
4. 升级成功/失败的评估结论
5. 如失败，提供回退建议
</output>
