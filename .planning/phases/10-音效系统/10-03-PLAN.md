---
phase: 10
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - src/composables/useGameControls.ts
  - src/stores/game.ts
  - src/App.vue
autonomous: false
requirements:
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
  - AUDIO-04
must_haves:
  truths:
    - "用户移动方块时播放移动音效"
    - "用户合并数字时播放合并音效"
    - "游戏胜利时播放胜利音效"
    - "游戏失败时播放失败音效"
    - "音效在用户首次交互后初始化"
  artifacts:
    - path: "src/composables/useGameControls.ts"
      provides: "集成音效的游戏控制逻辑"
      exports: ["useGameControls"]
    - path: "src/stores/game.ts"
      provides: "增强的游戏状态管理（支持音效触发）"
      exports: ["useGameStore"]
    - path: "src/App.vue"
      provides: "音效系统初始化（首次用户交互）"
      exports: ["default"]
  key_links:
    - from: "src/composables/useGameControls.ts"
      to: "src/composables/useAudio.ts"
      via: "useAudio import 和 play 调用"
      pattern: "audio\\.play\\("
    - from: "src/App.vue"
      to: "src/composables/useAudio.ts"
      via: "useAudio import 和 initialize 调用"
      pattern: "audio\\.initialize\\(\\)"
    - from: "src/stores/game.ts"
      to: "src/App.vue"
      via: "status 状态监听"
      pattern: "watch.*status"
---

<objective>
将音效系统集成到游戏流程中，实现移动、合并、胜利、失败音效，并处理浏览器自动播放策略。

目的：为用户提供完整的音频反馈，提升游戏体验。
输出：集成音效的游戏控制、状态监听、首次交互初始化。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-音效系统/10-RESEARCH.md
@.planning/phases/10-音效系统/10-01-PLAN.md
@.planning/phases/10-音效系统/10-02-PLAN.md
@src/composables/useGameControls.ts
@src/stores/game.ts
@src/App.vue
@src/components/GameContainer.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：在游戏控制中集成移动和合并音效</name>
  <files>src/composables/useGameControls.ts</files>
  <behavior>
    - useGameControls 已导入 useAudio
    - 移动方块时播放移动音效（AUDIO-01）
    - 合并数字时播放合并音效（AUDIO-02）
    - 音效播放逻辑与移动检测正确
  </behavior>
  <action>
    修改 src/composables/useGameControls.ts，在移动方块时播放音效。

    关键修改：
    1. **导入 useAudio**：
       在文件顶部添加：
       import { useAudio } from './useAudio'

    2. **在 composable 函数中初始化**：
       export function useGameControls(targetRef: Ref&lt;HTMLElement | undefined&gt;) {
         const store = useGameStore()
         const audio = useAudio()  // 新增

    3. **在键盘控制中播放音效**（handleKeydown 函数）：
       修改方向键处理逻辑：
       ```typescript
       const direction = keyMap[event.key]
       if (direction) {
         event.preventDefault()

         // 记录移动前的分数，用于检测是否合并
         const previousScore = store.score
         store.moveGrid(direction)

         // 检测是否发生合并（分数增加）
         if (store.score > previousScore) {
           // 有合并 → 播放合并音效
           audio.play('merge')
         } else {
           // 仅有移动 → 播放移动音效
           audio.play('move')
         }
       }
       ```

    4. **在触摸控制中播放音效**（useSwipe 的 onSwipe 回调）：
       同样的逻辑：
       ```typescript
       onSwipe() {
         // ... 现有方向计算逻辑 ...

         if (direction && store.status === 'playing') {
           const previousScore = store.score
           store.moveGrid(direction)

           // 播放音效
           if (store.score > previousScore) {
             audio.play('merge')
           } else {
             audio.play('move')
           }
         }
       }
       ```

    5. **在鼠标拖拽控制中播放音效**（handleMouseUp 函数）：
       同样的逻辑：
       ```typescript
       if (direction && store.status === 'playing') {
         const previousScore = store.score
         store.moveGrid(direction)

         // 播放音效
         if (store.score > previousScore) {
           audio.play('merge')
         } else {
           audio.play('move')
         }
       }
       ```

    完整实现参考 RESEARCH.md 第 287-332 行的"模式 3：游戏控制集成"。

    注意：
    - 通过分数变化判断是否合并（store.score > previousScore）
    - 移动音效：仅移动无合并
    - 合并音效：移动且有合并（分数增加）
    - 不修改现有控制逻辑，只添加音效播放
  </action>
  <verify>
    <automated>
      cat &lt;&lt;'EOF' | node --input-type=module
      import { readFileSync } from 'fs';
      const content = readFileSync('src/composables/useGameControls.ts', 'utf-8');
      const hasAudioImport = content.includes("useAudio") && content.includes("from './useAudio'");
      const hasAudioInit = content.includes('const audio = useAudio()');
      const hasMovePlay = content.includes("audio.play('move')") || content.includes('audio.play("move")');
      const hasMergePlay = content.includes("audio.play('merge')") || content.includes('audio.play("merge")');
      const hasScoreCheck = content.includes('previousScore');
      if (!hasAudioImport) throw new Error('Missing useAudio import');
      if (!hasAudioInit) throw new Error('Missing audio initialization');
      if (!hasMovePlay) throw new Error('Missing move sound playback');
      if (!hasMergePlay) throw new Error('Missing merge sound playback');
      if (!hasScoreCheck) throw new Error('Missing score change detection');
      console.log('PASS');
      EOF
    </automated>
  </verify>
  <done>游戏控制已集成移动和合并音效（AUDIO-01, AUDIO-02）</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：在应用中监听游戏状态并播放胜利/失败音效</name>
  <files>src/App.vue</files>
  <behavior>
    - App.vue 已导入 useAudio 和 useGameStore
    - 监听游戏状态变化（status）
    - 游戏胜利时播放胜利音效（AUDIO-03）
    - 游戏失败时播放失败音效（AUDIO-04）
    - 首次用户交互时初始化音效系统
  </behavior>
  <action>
    修改 src/App.vue，添加游戏状态监听和音效初始化。

    关键修改：
    1. **导入依赖**（在 &lt;script setup&gt; 中）：
       ```typescript
       import { watch, onMounted } from 'vue'
       import { useGameStore } from '@/stores/game'
       import { useAudio } from '@/composables/useAudio'
       ```

    2. **初始化 store 和 audio**：
       ```typescript
       const store = useGameStore()
       const audio = useAudio()
       ```

    3. **监听游戏状态变化**（在 setup 逻辑中添加）：
       ```typescript
       // 监听游戏状态变化，播放胜利/失败音效
       watch(() => store.status, (newStatus, oldStatus) => {
         // 只在从 PLAYING 状态变为 WON 或 LOST 时播放音效
         if (newStatus === 'won' && oldStatus === 'playing') {
           audio.play('win')
         } else if (newStatus === 'lost' && oldStatus === 'playing') {
           audio.play('lose')
         }
       })
       ```

    4. **处理浏览器自动播放策略**（在 onMounted 中添加）：
       ```typescript
       // 在首次用户交互时初始化音效系统
       function handleFirstInteraction() {
         if (!audio.isInitialized.value) {
           audio.initialize()
         }
       }

       onMounted(() => {
         // 监听首次用户交互（点击、按键、触摸）
         const events = ['click', 'keydown', 'touchstart'] as const
         events.forEach(event => {
           window.addEventListener(event, handleFirstInteraction, { once: true })
         })
       })
       ```

    完整实现参考 RESEARCH.md 第 334-361 行的"模式 4：游戏状态监听"和第 541-565 行的"初始化音效系统"。

    注意：
    - 使用 { once: true } 确保事件监听器只触发一次
    - 只在状态从 PLAYING 变为 WON/LOST 时播放音效（避免重复播放）
    - 初始化时机：首次用户交互（点击、按键、触摸）
    - 初始化检查：audio.isInitialized.value 避免重复初始化

    替代方案（如果 App.vue 不适合）：
    - 在 GameContainer.vue 中添加相同的逻辑
    - 选择逻辑最集中、生命周期最清晰的组件
  </action>
  <verify>
    <automated>
      cat &lt;&lt;'EOF' | node --input-type=module
      import { readFileSync } from 'fs';
      const content = readFileSync('src/App.vue', 'utf-8');
      const hasStoreImport = content.includes('useGameStore');
      const hasAudioImport = content.includes('useAudio');
      const hasWatch = content.includes('watch(() => store.status');
      const hasWinPlay = content.includes("audio.play('win')") || content.includes('audio.play("win")');
      const hasLosePlay = content.includes("audio.play('lose')") || content.includes('audio.play("lose")');
      const hasInit = content.includes('handleFirstInteraction') || content.includes('audio.initialize()');
      const hasOnceListener = content.includes('{ once: true }') || content.includes('once: true');
      if (!hasStoreImport) throw new Error('Missing game store import');
      if (!hasAudioImport) throw new Error('Missing audio composable import');
      if (!hasWatch) throw new Error('Missing status watch');
      if (!hasWinPlay) throw new Error('Missing win sound playback');
      if (!hasLosePlay) throw new Error('Missing lose sound playback');
      if (!hasInit) throw new Error('Missing audio initialization');
      if (!hasOnceListener) throw new Error('Missing once option for event listener');
      console.log('PASS');
      EOF
    </automated>
  </verify>
  <done>游戏状态监听已添加，胜利/失败音效已集成（AUDIO-03, AUDIO-04）</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>任务 3：验证音效系统集成</name>
  <files>public/sounds/sprite.mp3</files>
  <what-built>
    音效系统已完全集成到游戏流程中：
    - 移动方块时播放移动音效
    - 合并数字时播放合并音效
    - 游戏胜利时播放胜利音效
    - 游戏失败时播放失败音效
    - 音量控制 UI 可正常调节音量和静音
    - 音效设置持久化到 localStorage
  </what-built>
  <how-to-verify>
    1. **启动开发服务器**：
       ```bash
       pnpm dev
       ```

    2. **测试移动音效（AUDIO-01）**：
       - 访问 http://localhost:5173/2048/
       - 按方向键或滑动屏幕移动方块
       - 验证：每次有效移动播放移动音效

    3. **测试合并音效（AUDIO-02）**：
       - 移动方块导致数字合并
       - 验证：合并时播放合并音效（与移动音效不同）

    4. **测试胜利音效（AUDIO-03）**：
       - 使用浏览器开发者工具强制胜利（或玩到 2048）
       - 验证：游戏胜利时播放胜利音效

    5. **测试失败音效（AUDIO-04）**：
       - 让游戏达到无移动状态
       - 验证：游戏失败时播放失败音效

    6. **测试音量控制（AUDIO-05）**：
       - 点击音量按钮展开滑块
       - 拖动滑块调节音量
       - 验证：音效音量随滑块变化

    7. **测试静音功能（AUDIO-06）**：
       - 点击音量按钮静音/取消静音
       - 验证：静音时所有音效停止播放

    8. **测试持久化（AUDIO-07）**：
       - 调节音量或静音
       - 刷新页面（F5）
       - 验证：音量和静音状态保持不变

    9. **检查控制台**：
       - 确认没有音效加载错误
       - 确认 AudioContext 初始化成功

    10. **移动端测试**（可选）：
        - 在移动设备或浏览器移动模式测试
        - 验证：音效在移动端正常播放
        - 验证：音量控制 UI 在移动端正常显示

    常见问题：
    - 音效不播放：检查浏览器自动播放策略（确保有用户交互）
    - 音效加载失败：检查 public/sounds/sprite.mp3 文件是否存在
    - 音量控制不生效：检查 localStorage 是否正确存储
  </how-to-verify>
  <resume-signal>输入 "验证通过" 或描述问题</resume-signal>
</task>

</tasks>

<verification>
- useGameControls 已集成移动和合并音效
- App.vue 已添加游戏状态监听
- 胜利/失败音效在正确时机播放
- 音效系统在首次用户交互时初始化
- 浏览器自动播放策略已处理
- 所有音效场景经过验证（移动、合并、胜利、失败）
- 音量控制和持久化功能正常
</verification>

<success_criteria>
- [ ] 移动方块时播放移动音效（AUDIO-01）
- [ ] 合并数字时播放合并音效（AUDIO-02）
- [ ] 游戏胜利时播放胜利音效（AUDIO-03）
- [ ] 游戏失败时播放失败音效（AUDIO-04）
- [ ] 音效在首次用户交互后初始化
- [ ] 音量控制 UI 可正常调节音量和静音（AUDIO-05, AUDIO-06）
- [ ] 音效设置持久化到 localStorage（AUDIO-07）
- [ ] 控制台无音效加载错误
</success_criteria>

<output>
完成后创建 `.planning/phases/10-音效系统/10-03-SUMMARY.md`
</output>
