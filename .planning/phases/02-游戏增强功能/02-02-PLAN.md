---
phase: 02-游戏增强功能
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/stores/game.ts
  - src/core/__tests__/store.test.ts
autonomous: true
requirements:
  - ENHANCE-01

must_haves:
  truths:
    - "用户可以撤销上一步操作，恢复到移动前的状态"
    - "撤销时扣除合并获得的分数"
    - "撤销次数限制为 3 次"
    - "撤销次数用完后不能继续撤销"
    - "游戏结束后不能撤销"
  artifacts:
    - path: "src/stores/game.ts"
      provides: "撤销功能实现"
      exports: ["undo", "canUndo", "undoCount", "undoLimit"]
      min_lines: 130
    - path: "src/core/__tests__/store.test.ts"
      provides: "撤销功能测试"
      min_lines: 350
  key_links:
    - from: "src/stores/game.ts"
      to: "src/core/storage.ts"
      via: "导入存储工具"
      pattern: "import.*from '@/core/storage'"
    - from: "src/stores/game.ts"
      to: "history 数组"
      via: "pop() 操作恢复状态"
      pattern: "history\\.value\\.pop\\(\\)"
---

<objective>
在 Pinia store 中实现撤销功能，允许用户撤销上一步操作

目的：提升游戏可玩性，让玩家可以纠正错误操作
输出：完整的撤销功能实现，包含撤销限制和分数扣除机制
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-游戏增强功能/02-01-SUMMARY.md

# 已有 store 结构（来自 Phase 1）
```typescript
export const useGameStore = defineStore('game', () => {
  const grid = ref<Grid>(createEmptyGrid())
  const score = ref<number>(0)
  const status = ref<GameStatus>(GameStatus.IDLE)
  const history = ref<Grid[]>([])  // 已实现，存储移动前的状态

  function moveGrid(direction: Direction) {
    history.value.push([...grid.value])  // 已实现保存历史
    // ... 移动逻辑
  }
})
```

# 关键实现细节（来自 CONTEXT.md）
- 仅撤销一步
- 撤销时扣除合并获得的分数（需要记录每次移动的得分）
- 仅游戏中可撤销，游戏结束后不能撤销
- 限制 3 次撤销机会（默认值）
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现撤销功能</name>
  <files>src/stores/game.ts, src/core/__tests__/store.test.ts</files>
  <behavior>
    - 测试 1: 撤销后恢复到移动前的网格状态
    - 测试 2: 撤销时扣除该次移动获得的分数
    - 测试 3: 撤销后 undoCount 增加
    - 测试 4: 撤销次数达到限制后不能继续撤销
    - 测试 5: 游戏结束后不能撤销（status = LOST）
    - 测试 6: 没有历史记录时不能撤销
    - 测试 7: canUndo 计算属性正确反映是否可撤销
    - 测试 8: 新游戏时重置撤销次数
  </behavior>
  <action>
修改 src/stores/game.ts，实现撤销功能：

1. **新增状态**
```typescript
/** 撤销次数计数 */
const undoCount = ref<number>(0)

/** 撤销次数限制 */
const undoLimit = ref<number>(3)

/** 移动得分历史（与 history 数组对应，记录每次移动的得分） */
const scoreHistory = ref<number[]>([])
```

2. **新增计算属性**
```typescript
/** 是否可以撤销 */
const canUndo = computed<boolean>(() =>
  status.value === GameStatus.PLAYING &&
  history.value.length > 0 &&
  undoCount.value < undoLimit.value
)
```

3. **修改 moveGrid 方法**
- 在保存历史时，同时保存该次移动的得分
- 需要先计算得分，再保存到 scoreHistory
- 确保 history 和 scoreHistory 的索引对应

4. **新增 undo 方法**
```typescript
function undo() {
  // 1. 检查是否可以撤销
  if (!canUndo.value) {
    return
  }

  // 2. 从历史记录中恢复网格
  const previousGrid = history.value.pop()
  if (!previousGrid) {
    return
  }

  // 3. 恢复网格状态
  grid.value = previousGrid

  // 4. 扣除该次移动的得分
  const scoreDeduction = scoreHistory.value.pop() ?? 0
  score.value = Math.max(0, score.value - scoreDeduction)

  // 5. 增加撤销计数
  undoCount.value += 1

  // 6. 更新游戏状态（可能从 LOST 变回 PLAYING）
  if (status.value === GameStatus.LOST) {
    status.value = GameStatus.PLAYING
  }
}
```

5. **修改 reset 和 initialize 方法**
- 在 reset() 中重置 undoCount = 0
- 在 reset() 中清空 scoreHistory = []
- 在 initialize() 中调用 reset() 逻辑

6. **导出新增的状态和方法**
- undoCount, undoLimit, canUndo
- undo 方法

实现要求：
- 保持与现有代码风格一致
- 添加详细的中文注释
- 确保 scoreHistory 和 history 数组同步
  </action>
  <verify>
    <automated>npm test -- store.test.ts</automated>
  </verify>
  <done>
  - 所有 8+ 个测试用例通过
  - undo 方法正确恢复历史状态
  - 撤销时正确扣除分数
  - 撤销限制正常工作
  - 游戏结束后不能撤销
  - canUndo 计算属性正确反映可撤销状态
  </done>
</task>

</tasks>

<verification>
- 运行完整测试套件验证撤销功能
- 手动测试撤销流程：移动 → 撤销 → 验证状态恢复
- 验证撤销次数限制正常工作
- 确认游戏结束后不能撤销
</verification>

<success_criteria>
- 撤销功能完整实现并测试通过
- 撤销时正确扣除分数
- 撤销次数限制正常工作
- 边界情况处理正确（无历史、游戏结束、达到限制）
</success_criteria>

<output>
After completion, create `.planning/phases/02-游戏增强功能/02-02-SUMMARY.md`
</output>
