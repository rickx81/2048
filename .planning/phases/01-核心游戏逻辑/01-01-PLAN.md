---
phase: 01-核心游戏逻辑
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/types.ts
  - src/core/utils.ts
  - src/core/__tests__/types.test.ts
  - src/core/__tests__/utils.test.ts
autonomous: true
requirements:
  - GAME-01
must_haves:
  truths:
    - "游戏网格用 4x4 二维数组表示，0 表示空位"
    - "移动方向使用字符串字面量类型定义"
    - "游戏状态包含网格、分数和游戏状态枚举"
    - "可以正确创建空网格和初始网格"
    - "可以正确找到网格中的所有空位"
    - "可以随机选择空位并生成数字（2 或 4）"
  artifacts:
    - path: "src/core/types.ts"
      provides: "类型定义和接口"
      exports: ["Grid", "Direction", "GameState", "GameStatus"]
      min_lines: 20
    - path: "src/core/utils.ts"
      provides: "工具函数"
      exports: ["createEmptyGrid", "getEmptyCells", "generateRandomTile"]
      min_lines: 30
    - path: "src/core/__tests__/utils.test.ts"
      provides: "工具函数测试"
      min_lines: 50
  key_links:
    - from: "src/core/utils.ts"
      to: "src/core/types.ts"
      via: "import type"
      pattern: "import.*from.*['\"]./types['\"]"
    - from: "src/core/__tests__/utils.test.ts"
      to: "src/core/utils.ts"
      via: "import"
      pattern: "import.*from.*['\"]./utils['\"]"
---

<objective>
定义核心游戏数据结构和基础工具函数，为后续游戏逻辑提供类型安全和可复用的工具。

目的：建立游戏的数据模型基础，确保类型安全和代码可维护性。
输出：类型定义文件、工具函数模块和对应的单元测试。
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-核心游戏逻辑/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1: 定义游戏核心类型</name>
  <files>src/core/types.ts</files>
  <behavior>
    - 测试 1: Grid 类型应该是 4x4 的二维数组
    - 测试 2: Direction 类型应该包含四个方向
    - 测试 3: GameStatus 枚举应该包含 IDLE, PLAYING, WON, LOST
    - 测试 4: GameState 接口应该包含 grid, score, status
  </behavior>
  <action>
在 src/core/types.ts 中创建以下类型定义：

1. **Grid 类型**：使用类型别名定义 4x4 二维数组
   ```typescript
   export type Grid = number[][]; // 4x4 数组，0 表示空位
   ```

2. **Direction 类型**：使用字符串字面量联合类型
   ```typescript
   export type Direction = 'UP' | 'DOWN' | 'LEFT' | 'RIGHT';
   ```

3. **GameStatus 枚举**：定义游戏状态
   ```typescript
   export enum GameStatus {
     IDLE = 'idle',       // 游戏未开始
     PLAYING = 'playing', // 游戏进行中
     WON = 'won',         // 游戏胜利
     LOST = 'lost'        // 游戏结束
   }
   ```

4. **GameState 接口**：定义完整游戏状态
   ```typescript
   export interface GameState {
     grid: Grid;
     score: number;
     status: GameStatus;
   }
   ```

添加 JSDoc 注释说明每个类型的用途。
不要在这个文件中添加任何实现逻辑，只做类型定义。
  </action>
  <verify>
    <automated>npm run test:unit -- src/core/__tests__/types.test.ts</automated>
  </verify>
  <done>
类型文件创建完成，包含 Grid、Direction、GameStatus 和 GameState 定义，所有类型测试通过。
  </done>
</task>

<task type="auto" tdd="true">
  <name>任务 2: 实现基础工具函数</name>
  <files>src/core/utils.ts, src/core/__tests__/utils.test.ts</files>
  <behavior>
    - 测试 1: createEmptyGrid 返回 4x4 全 0 数组
    - 测试 2: createInitialGrid 在两个不同位置生成 2 或 4
    - 测试 3: getEmptyCells 返回所有值为 0 的坐标
    - 测试 4: getRandomEmptyCell 从空位数组中随机选择一个
    - 测试 5: generateRandomTile 返回 2（90%概率）或 4（10%概率）
    - 测试 6: addRandomTile 在指定位置设置随机值，返回新网格
  </behavior>
  <action>
在 src/core/utils.ts 中实现以下工具函数：

1. **createEmptyGrid()**: 创建 4x4 全 0 数组
   ```typescript
   export function createEmptyGrid(): Grid
   ```
   - 返回 4x4 数组，所有元素初始化为 0
   - 使用 Array.from 创建新数组，避免引用共享

2. **createInitialGrid()**: 创建初始游戏网格
   ```typescript
   export function createInitialGrid(): Grid
   ```
   - 调用 createEmptyGrid 创建基础网格
   - 在两个不同空位生成数字（2 或 4）
   - 使用 getRandomEmptyCell 和 generateRandomTile

3. **getEmptyCells(grid)**: 获取所有空位坐标
   ```typescript
   export function getEmptyCells(grid: Grid): [number, number][]
   ```
   - 遍历网格，返回所有值为 0 的坐标 [row, col]
   - 返回空数组如果没有空位

4. **getRandomEmptyCell(emptyCells)**: 随机选择一个空位
   ```typescript
   export function getRandomEmptyCell(emptyCells: [number, number][]): [number, number] | null
   ```
   - 使用 Math.random() 随机选择
   - 返回 null 如果空位数组为空

5. **generateRandomTile()**: 生成随机数字
   ```typescript
   export function generateRandomTile(): number
   ```
   - 90% 概率返回 2
   - 10% 概率返回 4
   - 使用 Math.random() < 0.9 判断

6. **addRandomTile(grid, position)**: 在指定位置添加随机数字
   ```typescript
   export function addRandomTile(grid: Grid, position: [number, number]): Grid
   ```
   - **不可变操作**：创建网格深拷贝，修改后返回新网格
   - 在指定位置设置随机数字（2 或 4）
   - 使用 generateRandomTile() 生成值
   - 使用 JSON.parse(JSON.stringify()) 或展开运算符实现深拷贝

所有函数必须是纯函数，不修改输入参数。
每个函数添加 JSDoc 注释说明参数和返回值。
  </action>
  <verify>
    <automated>npm run test:unit -- src/core/__tests__/utils.test.ts</automated>
  </verify>
  <done>
所有工具函数实现完成，单元测试全部通过，包括：
- 创建空网格和初始网格
- 查找和随机选择空位
- 生成随机数字（2 或 4）
- 在网格中添加数字（不可变操作）
  </done>
</task>

</tasks>

<verification>
运行所有测试确保实现正确：
```bash
npm run test:unit
```

验证要点：
- [ ] 所有类型定义正确且通过类型检查
- [ ] createEmptyGrid 返回 4x4 全 0 数组
- [ ] createInitialGrid 生成两个不同的非零数字
- [ ] getEmptyCells 正确识别所有空位
- [ ] 随机函数的统计分布大致符合预期（2:90%, 4:10%）
- [ ] 所有操作都是不可变的（不修改原数组）
- [ ] 代码覆盖率 100%
</verification>

<success_criteria>
1. 类型定义文件 src/core/types.ts 存在并导出所有必需类型
2. 工具函数文件 src/core/utils.ts 存在并实现所有函数
3. 所有单元测试通过（覆盖率 100%）
4. TypeScript 编译无错误
5. 所有函数都是纯函数（不可变操作）
6. 随机生成符合概率分布（2: 90%, 4: 10%）
</success_criteria>

<output>
完成后创建 `.planning/phases/01-核心游戏逻辑/01-01-SUMMARY.md`
</output>
