---
phase: 09-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/GameContainer.vue
  - src/components/GameHeader.vue
  - src/components/GameBoard.vue
  - src/components/Tile.vue
  - src/components/GameOverOverlay.vue
  - src/components/GameWonOverlay.vue
  - e2e/fixtures/game-fixtures.ts
  - e2e/pages/game-page.ts
  - e2e/vue.spec.ts
autonomous: true
requirements:
  - E2E-04
  - E2E-05

must_haves:
  truths:
    - "所有组件都有语义化 data-testid 属性，可被 Playwright 选择器定位"
    - "Page Object Model 类封装了游戏页面的所有交互操作"
    - "Playwright fixture 扩展提供游戏特定的测试工具"
    - "旧的占位测试被替换为真实游戏测试框架"
  artifacts:
    - path: "e2e/fixtures/game-fixtures.ts"
      provides: "Playwright fixture 扩展（游戏初始化、状态验证）"
      exports: ["test", "expect"]
    - path: "e2e/pages/game-page.ts"
      provides: "Page Object Model - 游戏页面交互封装"
      exports: ["GamePage"]
    - path: "e2e/vue.spec.ts"
      provides: "基础 E2E 测试（替换旧占位代码）"
      contains: "import { test, expect }"
  key_links:
    - from: "e2e/pages/game-page.ts"
      to: "src/components/*.vue"
      via: "data-testid 选择器"
      pattern: "data-testid="
    - from: "e2e/fixtures/game-fixtures.ts"
      to: "e2e/pages/game-page.ts"
      via: "fixture 注入 GamePage 实例"
      pattern: "GamePage"
---

<objective>
为所有 Vue 组件添加 data-testid 属性，建立 E2E 测试基础设施（Page Object Model + Fixtures），替换旧的占位测试。

Purpose: data-testid 是 E2E 测试的稳定选择器基础（E2E-05）；Page Object Model 和 Fixtures 是后续测试文件的复用基础（E2E-04）。先建立基础设施，后续测试文件直接消费。

Output: 7 个组件添加 data-testid 属性、GamePage POM 类、game-fixtures 扩展、基础冒烟测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-e2e/09-CONTEXT.md

@src/components/GameContainer.vue
@src/components/GameHeader.vue
@src/components/GameBoard.vue
@src/components/Tile.vue
@src/components/GameOverOverlay.vue
@src/components/GameWonOverlay.vue
@e2e/vue.spec.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在所有组件中添加 data-testid 属性</name>
  <files>
    src/components/GameContainer.vue
    src/components/GameHeader.vue
    src/components/GameBoard.vue
    src/components/Tile.vue
    src/components/GameOverOverlay.vue
    src/components/GameWonOverlay.vue
  </files>
  <read_first>
    src/components/GameContainer.vue
    src/components/GameHeader.vue
    src/components/GameBoard.vue
    src/components/Tile.vue
    src/components/GameOverOverlay.vue
    src/components/GameWonOverlay.vue
    .planning/phases/09-e2e/09-CONTEXT.md
  </read_first>
  <action>
按照 CONTEXT.md 中 "语义化扁平命名" 决策（D-16: `[组件名]-[元素名]` 或 `[功能描述]`），为以下组件添加 `data-testid` 属性：

**GameContainer.vue:**
- 最外层 div: `data-testid="game-container"`
- header 标签: `data-testid="game-header"`
- main 标签: `data-testid="game-board-wrapper"`
- GameOverOverlay: 已由子组件自己处理
- GameWonOverlay: 已由子组件自己处理

**GameHeader.vue:**
- 最外层 header: `data-testid="game-header"`（注意：外层已有 game-header，此处用 header 标签自带的语义即可）
- 标题 h1: `data-testid="title"`
- 分数框 "分数": `data-testid="score"` 放在显示分数的 span 上（包含 `{{ store.score }}` 的 span）
- 分数框 "最高分": `data-testid="high-score"` 放在显示最高分的 span 上（包含 `{{ store.highScore }}` 的 span）
- 撤销按钮: `data-testid="undo-btn"`
- 新游戏按钮: `data-testid="new-game-btn"`

**GameBoard.vue:**
- 最外层 div（.game-board）: `data-testid="game-board"`

**Tile.vue:**
- 最外层 div（.tile）: `data-testid="tile"` 并且添加动态属性 `:data-row="row"` `:data-col="col"` `:data-value="value"` 用于精确定位
  注意：value 为 0 时不显示文字，但 data-value 仍然需要

**GameOverOverlay.vue:**
- overlay-mask div: `data-testid="game-over-overlay"`
- 标题 h2: `data-testid="game-over-title"`
- 最终分数 span（包含 `{{ store.score }}`）: `data-testid="game-over-score"`
- 重试按钮: `data-testid="retry-btn"`

**GameWonOverlay.vue:**
- overlay-mask div: `data-testid="game-won-overlay"`
- 标题 h2: `data-testid="game-won-title"`
- 最终分数 span（包含 `{{ store.score }}`）: `data-testid="game-won-score"`
- 继续游戏按钮: `data-testid="continue-btn"`
- 新游戏按钮: `data-testid="new-game-btn-overlay"`

注意：不要修改任何现有 CSS 类名、样式或逻辑。只在现有元素上添加 `data-testid` 属性。
  </action>
  <verify>
    <automated>cd "D:/Projects/demo/games/2048" && grep -c 'data-testid=' src/components/GameContainer.vue src/components/GameHeader.vue src/components/GameBoard.vue src/components/Tile.vue src/components/GameOverOverlay.vue src/components/GameWonOverlay.vue</automated>
  </verify>
  <done>
    - GameContainer.vue 包含 data-testid="game-container"
    - GameHeader.vue 包含 data-testid="score", data-testid="high-score", data-testid="undo-btn", data-testid="new-game-btn"
    - GameBoard.vue 包含 data-testid="game-board"
    - Tile.vue 包含 data-testid="tile", :data-row, :data-col, :data-value
    - GameOverOverlay.vue 包含 data-testid="game-over-overlay", data-testid="retry-btn"
    - GameWonOverlay.vue 包含 data-testid="game-won-overlay", data-testid="continue-btn", data-testid="new-game-btn-overlay"
    - 所有现有功能不受影响，pnpm type-check 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 Page Object Model 和 Playwright Fixtures</name>
  <files>
    e2e/pages/game-page.ts
    e2e/fixtures/game-fixtures.ts
    e2e/vue.spec.ts
  </files>
  <read_first>
    playwright.config.ts
    e2e/vue.spec.ts
    .planning/phases/09-e2e/09-CONTEXT.md
  </read_first>
  <action>
创建 E2E 测试基础设施，实现 Page Object Model 模式和 Playwright fixture 扩展。

**1. 创建 `e2e/pages/game-page.ts` — GamePage 类：**

封装游戏页面的所有交互操作。使用 `@playwright/test` 的 `Page` 对象。所有选择器使用 `data-testid` 优先策略（per CONTEXT.md D-18）。

```typescript
import type { Page, Locator } from '@playwright/test'

export class GamePage {
  readonly page: Page

  // 核心元素定位器
  readonly gameContainer: Locator
  readonly gameBoard: Locator
  readonly scoreValue: Locator
  readonly highScoreValue: Locator
  readonly undoBtn: Locator
  readonly newGameBtn: Locator
  readonly gameOverOverlay: Locator
  readonly gameWonOverlay: Locator
  readonly retryBtn: Locator
  readonly continueBtn: Locator
  readonly newGameBtnOverlay: Locator

  constructor(page: Page) {
    this.page = page
    // 使用 data-testid 定位器
    this.gameContainer = page.getByTestId('game-container')
    this.gameBoard = page.getByTestId('game-board')
    this.scoreValue = page.getByTestId('score')
    this.highScoreValue = page.getByTestId('high-score')
    this.undoBtn = page.getByTestId('undo-btn')
    this.newGameBtn = page.getByTestId('new-game-btn')
    this.gameOverOverlay = page.getByTestId('game-over-overlay')
    this.gameWonOverlay = page.getByTestId('game-won-overlay')
    this.retryBtn = page.getByTestId('retry-btn')
    this.continueBtn = page.getByTestId('continue-btn')
    this.newGameBtnOverlay = page.getByTestId('new-game-btn-overlay')
  }

  // 导航方法
  async goto() {
    await this.page.goto('/')
  }

  // 获取指定位置的 tile
  getTile(row: number, col: number): Locator {
    return this.page.getByTestId('tile').filter({ has: this.page.locator(`[data-row="${row}"][data-col="${col}"]`) })
  }

  // 获取 tile 的值
  async getTileValue(row: number, col: number): Promise<number> {
    const tile = this.page.locator(`[data-testid="tile"][data-row="${row}"][data-col="${col}"]`)
    const value = await tile.getAttribute('data-value')
    return Number(value)
  }

  // 获取当前分数
  async getScore(): Promise<number> {
    const text = await this.scoreValue.textContent()
    return Number(text)
  }

  // 获取最高分
  async getHighScore(): Promise<number> {
    const text = await this.highScoreValue.textContent()
    return Number(text)
  }

  // 键盘操作
  async pressKey(direction: 'ArrowUp' | 'ArrowDown' | 'ArrowLeft' | 'ArrowRight') {
    await this.page.keyboard.press(direction)
  }

  // 触摸滑动操作
  async swipe(direction: 'up' | 'down' | 'left' | 'right', distance = 100) {
    const board = this.gameBoard
    const box = await board.boundingBox()
    if (!box) throw new Error('Game board not found')

    const centerX = box.x + box.width / 2
    const centerY = box.y + box.height / 2

    const moves = {
      up: { startX: centerX, startY: centerY + distance / 2, endX: centerX, endY: centerY - distance / 2 },
      down: { startX: centerX, startY: centerY - distance / 2, endX: centerX, endY: centerY + distance / 2 },
      left: { startX: centerX + distance / 2, startY: centerY, endX: centerX - distance / 2, endY: centerY },
      right: { startX: centerX - distance / 2, startY: centerY, endX: centerX + distance / 2, endY: centerY },
    }

    const move = moves[direction]
    await this.page.touchscreen.tap(move.startX, move.startY)
    // 使用 mouse 模拟滑动（touchscreen 不支持直接滑动序列）
    // 使用 pointer action API
    await this.page.mouse.move(move.startX, move.startY)
    await this.page.mouse.down()
    await this.page.mouse.move(move.endX, move.endY, { steps: 5 })
    await this.page.mouse.up()
  }

  // UI 操作
  async clickUndo() {
    await this.undoBtn.click()
  }

  async clickNewGame() {
    await this.newGameBtn.click()
  }

  async clickRetry() {
    await this.retryBtn.click()
  }

  async clickContinue() {
    await this.continueBtn.click()
  }

  // 状态检查
  async isGameOverVisible(): Promise<boolean> {
    return this.gameOverOverlay.isVisible()
  }

  async isGameWonVisible(): Promise<boolean> {
    return this.gameWonOverlay.isVisible()
  }

  // 获取棋盘上所有非零 tile 的数量
  async getNonZeroTileCount(): Promise<number> {
    let count = 0
    for (let row = 0; row < 4; row++) {
      for (let col = 0; col < 4; col++) {
        const value = await this.getTileValue(row, col)
        if (value > 0) count++
      }
    }
    return count
  }

  // 获取完整棋盘状态
  async getGridState(): Promise<number[][]> {
    const grid: number[][] = []
    for (let row = 0; row < 4; row++) {
      const rowData: number[] = []
      for (let col = 0; col < 4; col++) {
        rowData.push(await this.getTileValue(row, col))
      }
      grid.push(rowData)
    }
    return grid
  }
}
```

**2. 创建 `e2e/fixtures/game-fixtures.ts` — Playwright fixture 扩展：**

```typescript
import { test as base, expect } from '@playwright/test'
import { GamePage } from '../pages/game-page'

// 扩展 Playwright fixture，注入 GamePage 实例
export const test = base.extend<{ gamePage: GamePage }>({
  gamePage: async ({ page }, use) => {
    const gamePage = new GamePage(page)
    await gamePage.goto()
    await use(gamePage)
  },
})

export { expect }
```

**3. 替换 `e2e/vue.spec.ts` — 基础冒烟测试：**

删除旧的 "You did it!" 占位测试，替换为使用新基础设施的真实游戏冒烟测试：

```typescript
import { test, expect } from './fixtures/game-fixtures'

test.describe('游戏基础功能', () => {
  test('页面加载后显示游戏界面', async ({ gamePage }) => {
    // 验证核心元素可见
    await expect(gamePage.gameContainer).toBeVisible()
    await expect(gamePage.gameBoard).toBeVisible()
    await expect(gamePage.scoreValue).toBeVisible()
    await expect(gamePage.highScoreValue).toBeVisible()
  })

  test('游戏初始化时有 2 个方块', async ({ gamePage }) => {
    const tileCount = await gamePage.getNonZeroTileCount()
    expect(tileCount).toBe(2)
  })

  test('初始分数为 0', async ({ gamePage }) => {
    const score = await gamePage.getScore()
    expect(score).toBe(0)
  })

  test('新游戏按钮可以重置游戏', async ({ gamePage }) => {
    // 先做一次移动
    await gamePage.pressKey('ArrowRight')
    // 点击新游戏
    await gamePage.clickNewGame()
    // 验证分数重置
    const score = await gamePage.getScore()
    expect(score).toBe(0)
    // 验证只有 2 个方块
    const tileCount = await gamePage.getNonZeroTileCount()
    expect(tileCount).toBe(2)
  })
})
```

**目录创建：**
先创建 `e2e/pages/` 和 `e2e/fixtures/` 目录（如果不存在）。
  </action>
  <verify>
    <automated>cd "D:/Projects/demo/games/2048" && npx playwright test vue.spec.ts --project=chromium 2>&1 | tail -20</automated>
  </verify>
  <done>
    - e2e/pages/game-page.ts 存在，导出 GamePage 类
    - e2e/fixtures/game-fixtures.ts 存在，导出扩展的 test 和 expect
    - e2e/vue.spec.ts 替换为新冒烟测试，不再包含 "You did it!"
    - 4 个冒烟测试在 Chromium 上通过
    - pnpm type-check 通过
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'data-testid=' src/components/*.vue` — 每个组件至少有 1 个 data-testid
2. `npx playwright test vue.spec.ts --project=chromium` — 4 个冒烟测试通过
3. `pnpm type-check` — 类型检查通过
</verification>

<success_criteria>
- 所有 7 个 Vue 组件都有 data-testid 属性
- GamePage POM 类封装所有游戏交互
- Playwright fixture 注入 GamePage 实例
- 基础冒烟测试（页面加载、初始化方块、分数、新游戏）全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/09-e2e/09-01-SUMMARY.md`
</output>
