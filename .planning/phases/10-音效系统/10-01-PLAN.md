---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/composables/useAudio.ts
  - src/stores/audio.ts
  - src/core/types.ts
autonomous: true
requirements:
  - AUDIO-05
  - AUDIO-06
  - AUDIO-07
must_haves:
  truths:
    - "Howler.js 依赖已安装"
    - "音效状态通过 Pinia store 管理（音量、静音）"
    - "音效播放逻辑封装在 useAudio composable 中"
    - "音效设置持久化到 localStorage"
  artifacts:
    - path: "src/composables/useAudio.ts"
      provides: "音效播放逻辑封装"
      min_lines: 150
      exports: ["useAudio", "SoundEffect"]
    - path: "src/stores/audio.ts"
      provides: "音效状态管理（音量、静音）"
      min_lines: 40
      exports: ["useAudioStore"]
    - path: "src/core/types.ts"
      provides: "SoundEffect 类型定义"
      contains: "export type SoundEffect"
  key_links:
    - from: "src/stores/audio.ts"
      to: "localStorage"
      via: "@vueuse/core useStorage"
      pattern: "useStorage\\('__GAME_2048_AUDIO_"
    - from: "src/composables/useAudio.ts"
      to: "src/stores/audio.ts"
      via: "useAudioStore import"
      pattern: "import.*useAudioStore.*from.*@/stores/audio"
---

<objective>
创建音效系统核心基础设施，包括依赖安装、状态管理和播放逻辑封装。

目的：建立可复用的音效管理模块，为后续集成到游戏流程提供接口。
输出：Howler.js 依赖、useAudio composable、audio store、类型定义。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-音效系统/10-RESEARCH.md
@.planning/phases/10-音效系统/10-UI-SPEC.md
@.planning/REQUIREMENTS.md
@src/stores/game.ts
@src/composables/useTheme.ts
@src/core/storage.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任务 1：安装 Howler.js 依赖</name>
  <files>package.json, package-lock.json</files>
  <behavior>
    - Howler.js 2.2.4 已添加到 dependencies
    - package-lock.json 已更新
  </behavior>
  <action>
    运行：npm install howler@^2.2.4

    验证安装后，package.json 的 dependencies 应包含：
    "howler": "^2.2.4"

    注意：不使用 @vueuse/sound，直接使用 Howler.js + 自定义 composable（与研究文档一致）。
  </action>
  <verify>
    <automated>grep -q '"howler".*"2\.2\.4' package.json && grep -q 'howler' package-lock.json</automated>
  </verify>
  <done>Howler.js 2.2.4 已安装，package.json 和 package-lock.json 已更新</done>
</task>

<task type="auto" tdd="true">
  <name>任务 2：创建音效类型定义</name>
  <files>src/core/types.ts</files>
  <behavior>
    - SoundEffect 类型已导出（'move' | 'merge' | 'win' | 'lose'）
  </behavior>
  <action>
    在 src/core/types.ts 文件末尾添加音效类型定义：

    /**
     * 音效类型
     * 用于标识不同的游戏音效
     */
    export type SoundEffect = 'move' | 'merge' | 'win' | 'lose'

    位置：放在 GameStatus 相关类型之后，文件末尾 export 之前。
  </action>
  <verify>
    <automated>grep -q "export type SoundEffect = 'move' | 'merge' | 'win' | 'lose'" src/core/types.ts</automated>
  </verify>
  <done>SoundEffect 类型已在 src/core/types.ts 中定义并导出</done>
</task>

<task type="auto" tdd="true">
  <name>任务 3：创建音效状态管理 Store（audio.ts）</name>
  <files>src/stores/audio.ts</files>
  <behavior>
    - Pinia store 已创建
    - 音量状态通过 useStorage 持久化到 localStorage
    - 静音状态通过 useStorage 持久化到 localStorage
    - 提供 setVolume 和 toggleMute 方法
  </behavior>
  <action>
    创建 src/stores/audio.ts，内容参考 RESEARCH.md 的"模式 2：音效状态 Store"。

    关键要求：
    1. 使用 defineStore 定义 store
    2. 使用 @vueuse/core 的 useStorage 持久化音量和静音状态
    3. 存储键名：
       - 音量：__GAME_2048_AUDIO_VOLUME__（默认值 0.5，范围 0-1）
       - 静音：__GAME_2048_AUDIO_MUTED__（默认值 false）
    4. 提供 setVolume(newVolume: number) 方法，限制范围在 0-1
    5. 提供 toggleMute() 方法，切换静音状态
    6. 导出接口：volume, isMuted, setVolume, toggleMute

    完整实现参考 RESEARCH.md 第 243-286 行的示例代码。
  </action>
  <verify>
    <automated>
      cat &lt;&lt;'EOF' | node --input-type=module
      import { readFileSync } from 'fs';
      const content = readFileSync('src/stores/audio.ts', 'utf-8');
      const hasUseStorage = content.includes('useStorage');
      const hasStore = content.includes('defineStore');
      const hasVolumeKey = content.includes('__GAME_2048_AUDIO_VOLUME__');
      const hasMutedKey = content.includes('__GAME_2048_AUDIO_MUTED__');
      const hasExports = content.includes('volume') && content.includes('isMuted') && content.includes('setVolume') && content.includes('toggleMute');
      if (!hasStore) throw new Error('Missing defineStore');
      if (!hasUseStorage) throw new Error('Missing useStorage');
      if (!hasVolumeKey) throw new Error('Missing volume storage key');
      if (!hasMutedKey) throw new Error('Missing muted storage key');
      if (!hasExports) throw new Error('Missing required exports');
      console.log('PASS');
      EOF
    </automated>
  </verify>
  <done>audio store 已创建，音量和静音状态持久化到 localStorage</done>
</task>

<task type="auto" tdd="true">
  <name>任务 4：创建音效播放逻辑 Composable（useAudio.ts）</name>
  <files>src/composables/useAudio.ts</files>
  <behavior>
    - Howler.js 单例实例已封装
    - Audio Sprites 配置已定义（move, merge, win, lose）
    - 提供 initialize, play, setVolume, toggleMute, stopAll, unload 方法
    - 音量设置与 audio store 同步
  </behavior>
  <action>
    创建 src/composables/useAudio.ts，内容参考 RESEARCH.md 的"模式 1：音效 Composable"。

    关键要求：
    1. 导入 Howl 类：import { Howl } from 'howler'
    2. 定义 Audio Sprites 数据：
       const SPRITE_DATA = {
         move: [0, 150],      // 0ms-150ms
         merge: [200, 300],   // 200ms-500ms
         win: [600, 2000],    // 600ms-2600ms
         lose: [2700, 1500]   // 2700ms-4200ms
       }
    3. 使用全局单例模式（let soundInstance: Howl | null = null）
    4. 在 initialize() 中创建 Howl 实例：
       - src: ['/sounds/sprite.mp3']（音效文件路径）
       - sprite: SPRITE_DATA
       - volume: effectiveVolume（计算属性，静音时为 0）
       - preload: true
       - html5: false（优先使用 Web Audio API）
    5. 监听 'load' 事件设置 isInitialized.value = true
    6. 监听 'loaderror' 事件记录错误日志
    7. play(effect: SoundEffect) 方法：静音时不播放，否则调用 soundInstance.play(effect)
    8. 集成 audio store：
       - 导入 useAudioStore
       - 在 setVolume 和 toggleMute 中调用 store 方法
       - 使用 computed 计算有效音量（静音时为 0）
    9. unload() 方法：调用 soundInstance.unload() 并清理实例

    完整实现参考 RESEARCH.md 第 98-242 行的示例代码。

    注意：此任务只创建 composable，不创建音效文件（音效文件由任务 5 处理）。
  </action>
  <verify>
    <automated>
      cat &lt;&lt;'EOF' | node --input-type=module
      import { readFileSync } from 'fs';
      const content = readFileSync('src/composables/useAudio.ts', 'utf-8');
      const hasHowlImport = content.includes("from 'howler'") || content.includes('from "howler"');
      const hasSpriteData = content.includes('SPRITE_DATA') && content.includes('move') && content.includes('merge') && content.includes('win') && content.includes('lose');
      const hasSingleton = content.includes('soundInstance') && content.includes('null');
      const hasInitialize = content.includes('function initialize') || content.includes('const initialize');
      const hasPlay = content.includes('function play') || content.includes('const play');
      const hasUnload = content.includes('function unload') || content.includes('const unload');
      const hasStoreIntegration = content.includes('useAudioStore');
      if (!hasHowlImport) throw new Error('Missing Howler import');
      if (!hasSpriteData) throw new Error('Missing SPRITE_DATA');
      if (!hasSingleton) throw new Error('Missing singleton pattern');
      if (!hasInitialize) throw new Error('Missing initialize method');
      if (!hasPlay) throw new Error('Missing play method');
      if (!hasUnload) throw new Error('Missing unload method');
      if (!hasStoreIntegration) throw new Error('Missing audio store integration');
      console.log('PASS');
      EOF
    </automated>
  </verify>
  <done>useAudio composable 已创建，封装 Howler.js 播放逻辑并与 audio store 集成</done>
</task>

</tasks>

<verification>
- package.json 包含 howler@^2.2.4 依赖
- src/core/types.ts 导出 SoundEffect 类型
- src/stores/audio.ts 创建并通过 useStorage 持久化音量和静音状态
- src/composables/useAudio.ts 封装 Howler.js 播放逻辑，提供完整接口
- 所有文件使用 TypeScript 严格模式，类型安全
- 代码注释使用中文
- 遵循项目 FCIS 架构模式（Imperative Shell 层的副作用）
</verification>

<success_criteria>
- [ ] Howler.js 2.2.4 已安装并添加到 package.json
- [ ] SoundEffect 类型已在 core/types.ts 中定义
- [ ] audio store 已创建，音量和静音状态持久化到 localStorage（AUDIO-05, AUDIO-06, AUDIO-07）
- [ ] useAudio composable 已创建，封装音效播放逻辑
- [ ] Audio Sprites 配置已定义（move, merge, win, lose）
- [ ] 所有新增文件通过 TypeScript 类型检查
</success_criteria>

<output>
完成后创建 `.planning/phases/10-音效系统/10-01-SUMMARY.md`
</output>
